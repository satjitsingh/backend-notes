=== PAGE 1 ===
 
 
 

=== PAGE 2 ===
 
 
Copyright and Disclaimer 
Servlet by ByteMart 
Copyright © 2025 ByteMart. All rights reserved. 
 
Copyright Notice 
This book, Servlet by ByteMart, including all associated content—text, illustrations, code 
samples, diagrams, and examples—is the exclusive intellectual property of ByteMart. 
No part of this publication may be copied, stored, or transmitted in any form—electronic, 
mechanical, photocopied, recorded, or otherwise—without prior written permission from 
ByteMart. 
This copyright is protected under international intellectual property laws. Any unauthorized 
reproduction, sharing, or distribution of any part of this book is strictly prohibited and may 
result in legal action, including claims for damages and recovery of legal costs. 
For permission inquiries, please contact: 
Email: contact@bytemartdigital.in 
Website: bytemartdigital.in 
 
Disclaimer 
This book is provided "as is" without any guarantees or warranties, express or implied. While 
every effort has been made to ensure accuracy and completeness, ByteMart makes no 
representations regarding the currentness, performance, or applicability of the information. 
The contents are intended for educational purposes only. As technologies such as 
Hibernate are constantly evolving, readers are advised to consult the official documentation 
for the most up-to-date information and adapt the examples accordingly. 
 
Legal and Ethical Use Guidelines 
• Reproduction, republication, or unauthorized distribution of this book, in part or in full, 
is strictly prohibited. 
• Sharing this content on unauthorized platforms or using it for commercial purposes 
without written permission is illegal. 
• Using code or content from this book for unethical or unlawful activities is not 
permitted. 
 
    Warning 
This book is the result of significant expertise and effort. Any act of intellectual property 
theft—including plagiarism, unauthorized redistribution, or content misuse—will result in 
immediate legal action under applicable copyright and IP laws. 
ByteMart assumes no responsibility for any loss, damage, or consequences resulting from 
the use of the information or code provided in this publication. 
 
By Accessing or Using This Book, You Agree To: 
1. Respect the intellectual property rights of ByteMart. 
2. Use the content responsibly and ethically. 
3. Refrain from any activity that violates the copyright terms outlined in this document. 
ByteMart reserves the right to pursue strict legal action against any unauthorized use, 
duplication, or distribution of this material. 
Proceed responsibly. 
 
Contact Information 
For permissions, collaborations, or support inquiries: 
• Email: contact@bytemartdigital.in 
• Website: https://bytemartdigital.in

=== PAGE 3 ===
Servlet 1 
 
 
Servlet 
Table of Contents 
1. Introduction to Servlet 
 What is a Servlet? 
 Understanding the role of Servlet in web development. 
 Servlet vs. other web technologies. 
2. Web Server and Web Container 
 Understanding the relationship between web server and web container. 
 How web servers handle client requests. 
 Deployment descriptors. 
3. HTTP and HTTPS 
 Understanding HTTP and HTTPS protocols. 
 Differences between HTTP and HTTPS. 
 Importance of secure communication. 
4. TCP/IP and DNS 
 Overview of TCP/IP protocol suite. 
 Understanding DNS (Domain Name System). 
 How TCP/IP and DNS relate to Servlets. 
5. Setting up a Web Server 
 Configuring a web server environment. 
 Deployment options for Servlets. 
6. Servlet API 
 Overview of Servlet API. 
 Key interfaces and classes in Servlet API. 
 Servlet containers and their implementations. 
7. Servlet Example using web.xml 

=== PAGE 4 ===
Servlet 2 
 
 
 Writing Servlets using deployment descriptors (web.xml). 
 Configuring  Servlet mappings. 
 Handling Servlet initialization parameters. 
8. Servlet Example using Annotations 
 Writing Servlets using annotations. 
 Configuring Servlet mappings using annotations. 
 Handling Servlet initialization parameters using annotations. 
9. Servlet Life Cycle 
 Understanding the life cycle of a Servlet. 
 Initialization, service, and destruction phases. 
 Handling life cycle events. 
10. Servlet Parameters 
 Handling request parameters in Servlets. 
 Retrieving request parameters. 
 Dealing with parameter encoding and decoding. 
11. Request Dispatcher 
 Forwarding requests using RequestDispatcher. 
 Including content from other resources. 
 Redirecting requests. 
12. Servlet Thread Models 
 Understanding  single-threaded vs. multi-threaded models. 
 Thread safety considerations in Servlet programming. 
13. HttpServletRequest & HttpServletResponse 
 Understanding  HttpServletRequest  and HttpServletResponse  objects. 
 Manipulating request and response headers. 
 Accessing request attributes and session information. 
14. Session Management 
 Exploring HttpSession for session management. 

=== PAGE 5 ===
Servlet 3 
 
 
 Handling session attributes. 
 Session tracking techniques. 
15. Servlet Scopes 
 Understanding different scopes in Servlets (request, session, 
application). 
 Managing data across different scopes. 
16. Servlet Filter & Filter Chaining 
 Introduction to Servlet filters. 
 Implementing and configuring Servlet filters. 
 Filter chaining and order of execution. 
17. Listeners 
 Understanding  Servlet listeners. 
 Implementing ServletContextListener, ServletRequestListener, and 
HttpSessionListener. 
 Handling  application -wide events. 
18. Advanced Topics and Techniques 
 Asynchronous Servlets. 
 WebSockets and Servlets. 
 Handling file uploads. 
 Integrating Servlets with other Java EE technologies. 
19. Best Practices and Security 
 Best practices for Servlet programming. 
 Servlet security considerations. 
 Preventing common security vulnerabilities. 
20. Case Studies and Projects 
 Building Servlet-based web applications. 
 Integrating Servlets with larger projects. 
21. References and Further Learning 

=== PAGE 6 ===
Servlet 4 
 
 
 Recommended books, websites, and resources for further learning 
about Servlets. 
 
Chapter 1: Introduction to Servlet 
1 . 1 What is a Servlet? 
A Servlet is a Java programming language class used to extend the capabilities 
of servers hosting applications accessed by means of a request -response 
programming model. Servlets are commonly used to process or store a Java 
class's data in a web application, handle client requests, and generate dynamic 
web content. 
Key Points: 
 Java-based: Servlets are written in Java and are part of the Java EE 
(Enterprise Edition) platform. 
 Server-side component: They run on the server side, typically within a web 
server or application server. 
 Request-response model: Servlets operate on the request-response 
model, usually dealing with HTTP requests.  
Example: 
A simple Servlet that responds with "Hello, World!": 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
@WebServlet("/hello") 
public class HelloWorldServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
response.setContentType("text/html"); 

=== PAGE 7 ===
Servlet 5 
 
 
 
 
 
 
 
1 . 2 Understanding the Role of Servlet in Web Development 
Servlets play a crucial role in web development by acting as a middle layer 
between client requests and server responses. They can handle various tasks, 
such as processing form data, managing sessions, and generating dynamic 
content. 
Key Points: 
 Processing data: Servlets can read data sent by clients (such as form 
data), process it, and send back a response.  
 Session management: They can track and manage user sessions, ensuring 
a consistent user experience across multiple requests.  
 Dynamic content generation: Servlets can generate dynamic web content, 
such as HTML, XML, or JSON, based on user input or server -side logic. 
Example: 
Handling form data with a Servlet: 
 
 
 
 
 
 
 
 
 
 
 
 
 
1 . 3 Servlet vs. Other Web Technologies 
response.getWriter().println("<h1>Hello, World!</h1 
>"); 
} 
} 
@WebServlet("/submitForm") 
public class FormServlet extends HttpServlet { 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) 
throws ServletException, IOException { 
String name = request.getParameter("name"); 
response.setContentType("text/html"); 
response.getWriter().println("<h1>Hello, " + name + 
"!</h1>"); 
} 
} 

=== PAGE 8 ===
Servlet 6 
 
 
While Servlets are powerful and flexible, they are often compared with other 
web technologies such as JSP (JavaServer Pages), PHP, and ASP.NET. Each 
technology has its strengths and use cases.  
Key Points: 
 Servlets vs. JSP: JSP is a higher-level abstraction that allows embedding 
Java code directly in HTML, making it easier to write web pages. Servlets 
are more powerful and flexible but require more boilerplate code for 
generating HTML. 
 Servlets vs. PHP: PHP is a scripting language that is easier to use for 
beginners and is widely used for web development. Servlets, being Java- 
based, offer better performance and scalability for large, enterprise-level 
applications. 
 Servlets vs. ASP.NET: ASP.NET is a Microsoft technology for building 
dynamic web applications. It provides a rich set of tools and libraries, but 
Servlets have the advantage of being platform-independent and part of the 
open-source Java ecosystem. 
Example: 
Comparing a simple "Hello, World!" example in Servlet and JSP: 
Servlet: 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
JSP: 
@WebServlet("/hello") 
public class HelloWorldServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
response.setContentType("text/html"); 
response.getWriter().println("<h1>Hello, World!</h1 
>"); 
} 
} 
<%@ page contentType="text/html" pageEncoding="UTF-8"%> 
<!DOCTYPE html> 

=== PAGE 9 ===
Servlet 7 
 
 
 
 
 
 
 
 
 
 
 
Conclusion 
Servlets are a fundamental part of Java web development, providing a robust 
and flexible way to handle client requests and generate dynamic content. 
Understanding the role of Servlets, how they differ from other web 
technologies, and their practical applications sets a strong foundation for 
further exploration into web development with Java. 
 
Chapter 2: Web Server and Web Container 
2 . 1 Understanding the Relationship Between Web Server and 
Web Container 
What is a Web Server? 
A web server is a software or hardware system responsible for serving web 
pages to clients over the internet or an intranet. It handles HTTP requests from 
clients (browsers) and responds with the requested web pages or resources. 
Key Points: 
 Handles HTTP requests: Web servers accept requests from clients and 
return the requested web pages or resources.  
 Static content: Typically serves static content like HTML, CSS, JavaScript, 
and images. 
 Examples: Apache HTTP Server, Nginx, Microsoft IIS. 
 
What is a Web Container (Servlet Container)? 
A web container, also known as a servlet container, is a part of a web server or 
application server that manages the lifecycle of servlets, maps URLs to 
<html> 
<head> 
<title>Hello World</title> 
</head> 
<body> 
<h1>Hello, World!</h1> 
</body> 
</html> 

=== PAGE 10 ===
Servlet 8 
 
 
servlets, and ensures that the web application's servlet code conforms to the 
Java EE specifications. 
Key Points: 
 Manages servlets: Handles the lifecycle of servlets including their creation, 
initialization, service, and destruction. 
 Dynamic content: Facilitates the generation of dynamic content using 
servlets and JSP. 
 Examples: Apache Tomcat, Jetty, GlassFish. 
 
Relationship between Web Server and Web Container 
The web server and web container work together to handle client requests and 
generate dynamic web content. 
Key Points: 
 Static and dynamic content: The web server handles static content, while 
the web container handles dynamic content through servlets. 
 Integration: Some web servers, like Apache Tomcat, integrate both web 
server and web container functionalities. 
 Request flow: The web server routes requests for dynamic content to the 
web container, which processes the requests and generates the 
appropriate responses. 
Example: 
A request flow in a combined web server and web container setup: 
1. The client sends an HTTP request to the web server. 
2. The web server determines if the request is for static or dynamic content. 
3. If the request is for dynamic content, the web server forwards the request 
to the web container. 
4. The web container processes the request using the appropriate servlet. 
5. The servlet generates a response and sends it back to the web server. 
6. The web server sends the response to the client. 
 
2 . 2 How Web Servers Handle Client Requests 

=== PAGE 11 ===
Servlet 9 
 
 
 
 
Web servers handle client requests through a well-defined process involving 
several steps: 
1. Receive Request: The web server receives an HTTP request from a client 
(browser). 
2. Parse Request: The server parses the HTTP request to understand the 
requested resource and the HTTP method (GET, POST, etc.). 
3. Route Request: The server routes the request to the appropriate handler 
based on the requested resource. For static resources, it retrieves the file 
from the filesystem. 
4. Process Request: If the request is for dynamic content, the server forwards 
it to the web container for processing by a servlet. 
5. Generate Response: The server or web container generates the 
appropriate HTTP response. 
6. Send Response: The server sends the HTTP response back to the client. 
 
Example: 
Handling a simple GET request in a web server: 
 
1. Client sends a GET request for 
2. Web server parses the request and identifies 
resource. 
. 
as the requested 
3. Server retrieves from the filesystem. 
4. Server generates an HTTP response with the content of . 
5. Server sends the response back to the client, displaying the HTML page in 
the browser. 
2 . 3 Deployment Descriptors 
Deployment descriptors are configuration files used to describe how a web 
application should be deployed on a server. In Java EE, the deployment 
descriptor is an XML file named 
web application. 
Key Points: 
located in the directory of a 
 Configuration: Defines servlets, servlet mappings, initialization parameters, 
and other configuration details. 
 
 
 
 WEB-INF 

=== PAGE 12 ===
Servlet 10 
 
 
web.xml 
 
 Standardization: Provides a standardized way to configure web 
applications. 
 Compatibility: Ensures compatibility across different servlet containers. 
Example of : 
 
<web-app xmlns="<http://xmlns.jcp.org/xml/ns/javaee>" 
xmlns:xsi="<http://www.w3.org/2001/XMLSchema-insta 
nce>" 
avaee> 
 
xsi:schemaLocation="<http://xmlns.jcp.org/xml/ns/j 
 
<http://xmlns.jcp.org/xml/ns/j 
avaee/web-app_3_1.xsd>" 
version="3.1"> 
 
<servlet> 
<servlet-name>HelloServlet</servlet-name> 
<servlet-class>com.example.HelloServlet</servlet-cl 
ass> 
</servlet> 
 
<servlet-mapping> 
<servlet-name>HelloServlet</servlet-name> 
<url-pattern>/hello</url-pattern> 
</servlet-mapping> 
 
<welcome-file-list> 
<welcome-file>index.html</welcome-file> 
</welcome-file-list> 
</web-app> 
 
 
Explanation: 
 : Defines a servlet with a name and the fully qualified class name. 
: Maps a URL pattern to the servlet. 
: Specifies the default files to be served if no specific 
resource is requested. 
<servlet-mapping> 
<welcome-file-list> 

=== PAGE 13 ===
Servlet 11 
 
 
Conclusion 
Understanding the relationship between web servers and web containers is 
essential for developing and deploying web applications. Web servers handle 
client requests and serve static content, while web containers manage servlets 
and generate dynamic content. Deployment descriptors provide a standardized 
way to configure web applications, ensuring compatibility and ease of 
deployment. 
 
 
Chapter 3: HTTP and HTTPS 
3 . 1 Understanding HTTP and HTTPS Protocols 
What is HTTP? 
HTTP (Hypertext Transfer Protocol) is the foundational protocol for data 
communication on the World Wide Web. It defines how messages are formatted 
and transmitted, and how web servers and browsers should respond to various 
commands. 
Key Points: 
 Stateless protocol: Each request from a client to server is independent; the 
server retains no information about previous requests. 
 Text-based protocol: HTTP messages (requests and responses) are plain 
text. 
 Methods: Common methods include GET (retrieve data), POST (submit 
data), PUT (update data), DELETE (remove data). 
HTTP Request Example: 
 
 
 
 
 
 
HTTP Response Example: 
GET /index.html HTTP/1.1 
Host: www.example.com 
User-Agent: Mozilla/5.0 
Accept: text/html 

=== PAGE 14 ===
Servlet 12 
 
 
 
 
 
 
 
 
 
 
 
 
 
What is HTTPS? 
HTTPS (Hypertext Transfer Protocol Secure) is the secure version of HTTP. It 
uses SSL/TLS (Secure Sockets Layer/Transport Layer Security) to encrypt the 
data being transferred, ensuring that it cannot be read or tampered with by 
third parties. 
Key Points: 
 Encryption: Data is encrypted using SSL/TLS. 
 Authentication: Verifies the identity of the server, ensuring the client is 
communicating with the intended server. 
 Integrity: Ensures that data cannot be altered during transfer. 
 
HTTPS Request Example: 
The structure of an HTTPS request is the same as an HTTP request, but it is 
encrypted. 
3 . 2 Differences Between HTTP and HTTPS 
 
Feature HTTP HTTPS 
Port 80 443 
Security No encryption SSL/TLS encryption 
Speed Faster due to no 
encryption 
Slightly slower due to encryption 
overhead 
Use Case Non-sensitive data Sensitive data (login info, payment 
details) 
SEO No ranking boost Search engines prefer HTTPS 
HTTP/1.1 200 OK 
Content-Type: text/html 
Content-Length: 1234 
<!DOCTYPE html> 
<html> 
<head><title>Example</title></head> 
<body><h1>Hello, World!</h1></body> 
</html> 

=== PAGE 15 ===
Servlet 13 
 
 
Example: 
An HTTP request and its equivalent HTTPS request look similar to the user but 
are vastly different in terms of security. 
HTTP Request URL: 
 
 
 
HTTPS Request URL: 
 
 
3 . 3 Importance of Secure Communication 
Confidentiality 
Data transmitted over HTTPS is encrypted, preventing eavesdroppers from 
reading sensitive information such as login credentials, personal details, and 
payment information. 
Integrity 
HTTPS ensures that data sent and received is not altered during transfer. It 
provides mechanisms to detect if data has been tampered with or corrupted. 
Authentication 
HTTPS uses digital certificates to verify the identity of websites, helping to 
prevent man-in-the-middle attacks where attackers could impersonate a 
website. 
Trust and SEO 
Browsers indicate secure connections with padlock icons, providing users with 
a sense of security. Search engines like Google also give preference to HTTPS 
websites in search rankings, making it essential for SEO. 
Example: 
Using HTTPS for a login form: 
<http://www.example.com/login> 
<https://www.example.com/login> 

=== PAGE 16 ===
Servlet 14 
 
 
 
 
 
 
 
 
 
 
 
 
Conclusion 
Understanding HTTP and HTTPS protocols is fundamental for web 
development. HTTP is the basic protocol for web communication, while HTTPS 
adds an essential layer of security through encryption, integrity, and 
authentication. The shift towards HTTPS is critical for protecting sensitive data, 
building user trust, and improving SEO rankings. 
This is the detailed outline and content for Chapter 3. Let me know if you need 
any modifications or additional information. 
 
Chapter 4: TCP/IP and DNS 
4 . 1 Overview of TCP/IP Protocol Suite 
What is TCP/IP? 
TCP/IP (Transmission Control Protocol/Internet Protocol) is a set of 
communication protocols used to interconnect network devices on the internet. 
TCP/IP specifies how data is exchanged over the internet by providing end-to- 
end communications that identify how it should be broken into packets, 
addressed, transmitted, routed, and received at the destination. 
Key Points: 
 Layered Architecture: TCP/IP is organized into four layers: Application, 
Transport, Internet, and Network Interface. 
 Standards: It is a standardized protocol that is used globally for internet 
communications. 
<form action="<https://www.example.com/login>" method="pos 
t"> 
<label for="username">Username:</label> 
<input type="text" id="username" name="username"> 
<label for="password">Password:</label> 
<input type="password" id="password" name="password"> 
<input type="submit" value="Login"> 
</form> 

=== PAGE 17 ===
Servlet 15 
 
 
TCP/IP Layers: 
1. Application Layer: Provides protocols for specific data communications 
services on a process-to-process level, such as HTTP, FTP, SMTP.  
2. Transport Layer: Ensures complete data transfer and provides error 
checking. The main protocols are TCP (Transmission Control Protocol) and 
UDP (User Datagram Protocol). 
3. Internet Layer: Deals with packet forwarding, including routing through 
intermediate routers. The main protocol is IP (Internet Protocol). 
4. Network Interface Layer: Responsible for the physical transmission of data 
over network hardware. 
Example: 
Sending an HTTP request using TCP/IP: 
1. Application Layer: HTTP request is created. 
2. Transport Layer: TCP breaks the HTTP request into packets. 
3. Internet Layer: IP addresses the packets and routes them to the 
destination. 
4. Network Interface Layer: Physical network hardware transmits the packets. 
 
TCP vs. UDP: 
 TCP: Connection-oriented, reliable, ensures data integrity, used for 
applications where data delivery must be accurate (e.g., web browsing, 
email). 
 UDP: Connectionless, faster but less reliable, used for applications where 
speed is crucial and some data loss is acceptable (e.g., video streaming, 
online gaming). 
4 . 2 Understanding DNS (Domain Name System) 
What is DNS? 
DNS (Domain Name System) is a hierarchical system that translates human- 
readable domain names (like www.example.com) into IP addresses (like 
192.0.2.1) that computers use to identify each other on the network. 
Key Points: 

=== PAGE 18 ===
Servlet 16 
 
 
 
 
 Hierarchy: DNS operates in a hierarchical structure with root servers, top- 
level domains (TLDs), and authoritative name servers. 
 Records: DNS stores various types of records, such as A (Address), MX 
(Mail Exchange), CNAME (Canonical Name), and TXT (Text) records. 
 Resolvers: DNS resolvers (typically provided by ISPs) handle the process 
of converting domain names into IP addresses. 
Example: 
Resolving a domain name: 
1. A user types into a browser. 
2. The browser sends a DNS query to the local DNS resolver. 
3. The resolver queries root DNS servers, TLD servers, and authoritative name 
servers. 
4. The authoritative server for returns the IP address for 
. 
5. The resolver returns the IP address to the browser, which can then access 
the web server. 
Types of DNS Records: 
 A Record: Maps a domain name to an IPv4 address. 
 AAAA Record: Maps a domain name to an IPv6 address. 
 CNAME Record: Alias for another domain name. 
 MX Record: Specifies the mail servers for a domain. 
 TXT Record: Holds arbitrary text data for a domain. 
4 . 3 How TCP/IP and DNS Relate to Servlets 
TCP/IP and Servlets 
Servlets rely on TCP/IP for communication between clients and servers. When 
a client sends an HTTP request to a servlet, the following steps occur: 
1. The clientʼs browser creates an HTTP request. 
2. The HTTP request is encapsulated in TCP packets. 
 

=== PAGE 19 ===
Servlet 17 
 
 
 
 
3. The TCP packets are sent over the internet using IP addresses to route 
them to the server. 
4. The server receives the packets, reassembles them into the HTTP request, 
and the servlet processes the request. 
DNS and Servlets 
DNS plays a crucial role in directing HTTP requests to the correct server 
hosting the servlet. When a user accesses a web application using a domain 
name, DNS resolution steps include: 
1. The browser queries DNS to get the IP address associated with the domain 
name. 
2. The IP address is used to establish a TCP connection to the server. 
3. The server processes the request with the servlet and sends back the 
response. 
Example Scenario: 
A user wants to access a web application at . 
1. The user enters the URL into the browser. 
2. The browser performs a DNS lookup to find the IP address of 
. 
3. DNS resolver returns the IP address (e.g., 192.0.2.123). 
4. The browser establishes a TCP connection to 192.0.2.123. 
5. The browser sends an HTTP GET request for . 
6. The web server receives the request and forwards it to the servlet. 
7. The servlet processes the request and generates a login page. 
8. The web server sends the login page back to the client via TCP/IP. 
 
Conclusion 
Understanding TCP/IP and DNS is fundamental for developing and deploying 
web applications. TCP/IP provides the underlying communication framework 
for data exchange over the internet, while DNS translates user-friendly domain 
names into IP addresses, enabling users to access web applications 
 

=== PAGE 20 ===
Servlet 18 
 
 
 
seamlessly. These technologies ensure reliable and efficient communication 
between clients and servers, forming the backbone of the internet. 
 
Chapter 5: Setting up a Web Server 
5 . 1 Configuring a Web Server Environment 
Setting up a web server involves selecting the appropriate software, 
configuring it, and deploying your web application. Here, we will use Apache 
Tomcat as an example, which is a widely used open-source servlet container 
and web server. 
Step-by-Step Guide: 
1. Download and Install Java Development Kit (JDK): 
 Ensure you have the latest JDK installed. You can download it from 
Oracle's official site. 
2. Download Apache Tomcat: 
 Visit the Apache Tomcat website and download the latest version. 
3. Install Apache Tomcat: 
 Extract the downloaded archive to a directory of your choice. 
4. Set Environment Variables: 
 
 Set the 
directory. 
 Optionally, set 
directory. 
environment variable to point to your JDK installation 
to point to your Tomcat installation 
5. Start Tomcat Server: 
 Navigate to the directory inside your Tomcat installation and 
execute the (Windows) or (Linux/Mac)  script. 
 
 Verify the server is running by accessing 
web browser. 
Example: 
in your 
 
 
 
  
 

=== PAGE 21 ===
Servlet 19 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
5 . 2 Deployment Options for Servlets 
Once your web server is set up, you need to deploy your servlets. Deployment 
involves placing your web application's files in the appropriate directory 
structure so that the web server can find and execute them. 
 
Deployment Directory Structure: 
A typical web application directory structure for Tomcat looks like this: 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 WEB-INF: Contains web.xml and server-side classes. 
 web.xml: Deployment descriptor file. 
 classes: Contains compiled Java classes. 
 
Deploying a Web Application: 
1. Create Directory Structure: 
# On Windows: 
set JAVA_HOME=C:\\path\\to\\jdk 
set CATALINA_HOME=C:\\path\\to\\tomcat 
cd %CATALINA_HOME%\\bin 
startup.bat 
# On Linux/Mac: 
export JAVA_HOME=/path/to/jdk 
export CATALINA_HOME=/path/to/tomcat 
cd $CATALINA_HOME/bin 
./startup.sh 
webapps/ 
├── myapp/ 
│ 
│ 
│ 
│ 
│ 
│ 
│ 
├── WEB-INF/ 
│ 
│ 
│ 
│ 
│ 
├── web.xml 
└── classes/ 
└── com/ 
└── example/ 
└── HelloWorldServlet.class 
└── index.html 

=== PAGE 22 ===
Servlet 20 
 
 
WEB-INF/classes 
WEB-INF/web.xml 
 Create a directory under 
). 
with your application's name (e.g., 
 
 Inside , create and . 
 
2. Compile Servlet: 
 Compile your servlet code and place the compiled 
. 
 
 
files inside 
3. Create web.xml: 
 Define your servlets and mappings in . 
 
Example web.xml: 
 
<web-app xmlns="<http://xmlns.jcp.org/xml/ns/javaee>" 
xmlns:xsi="<http://www.w3.org/2001/XMLSchema-insta 
nce>" 
xsi:schemaLocation="<http://xmlns.jcp.org/xml/ns/j 
avaee> 
<http://xmlns.jcp.org/xml/ns/j 
avaee/web-app_3_1.xsd>" 
version="3.1"> 
 
<servlet> 
<servlet-name>HelloWorldServlet</servlet-name> 
<servlet-class>com.example.HelloWorldServlet</servl 
et-class> 
</servlet> 
 
<servlet-mapping> 
<servlet-name>HelloWorldServlet</servlet-name> 
<url-pattern>/hello</url-pattern> 
</servlet-mapping> 
 
</web-app> 
 
1. Access Your Application: 
 Restart Tomcat. 
 
 
 
WEB-INF 
 
WEB-INF/classes 

=== PAGE 23 ===
Servlet 21 
 
 
 
 Access your servlet by navigating to . 

=== PAGE 24 ===
Servlet 22 
 
 
Example HelloWorldServlet: 
 
package com.example; 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/hello") 
public class HelloWorldServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
response.setContentType("text/html"); 
response.getWriter().println("<h1>Hello, World!</h1 
>"); 
} 
} 
 
5 . 3 Troubleshooting and Tips 
Common Issues: 
1. Port Conflicts: 
 Ensure the port Tomcat is configured to use (default is 8080) is not 
being used by another application. 
2. Environment Variables: 
 
 Verify that 
3. Permissions: 
and are set correctly. 
 
 
 
 
 
Tips: 
Ensure that your user account has the necessary permissions to 
read/write the Tomcat installation directory and deploy applications. 
 
  

=== PAGE 25 ===
Servlet 23 
 
 
 Logging: Check the 
error messages and logs. 
directory in the Tomcat installation for detailed 
 
 Configuration: Customize the 
server settings like ports and resource limits. 
file in Tomcat to change 
 Security: For production environments, ensure proper security 
configurations are in place, such as restricting access to the manager 
application and disabling directory listings. 
Conclusion 
Setting up a web server is a critical step in deploying web applications. By 
configuring the environment, deploying servlets, and troubleshooting common 
issues, you ensure that your web server is running smoothly and securely. 
Understanding these steps will help you effectively manage and deploy your 
Java web applications. 
 
Chapter 6: Servlet API 
6 . 1 Overview of Servlet API 
The Servlet API provides the necessary interfaces and classes for creating and 
managing servlets. It is part of the Java EE (Enterprise Edition) platform, and it 
defines a standard for implementing web components that generate dynamic 
content. 
Key Components of the Servlet API: 
1. Servlet Interface: Defines the methods that all servlets must implement. 
2. HttpServlet Class: A convenience class that simplifies the development of 
HTTP-based servlets by providing methods to handle common HTTP 
requests. 
3. ServletConfig Interface: Provides servlet configuration information from 
the web container. 
4. ServletContext Interface: Enables servlets to communicate with their 
servlet container. 
5. ServletRequest Interface: Encapsulates client request information. 
 
 

=== PAGE 26 ===
Servlet 23 
 
 
 
6. ServletResponse Interface: Encapsulates the response information sent 
back to the client. 
7. HttpSession Interface: Manages user sessions. 
6 . 2 Key Interfaces and Classes in Servlet API 
Servlet Interface 
The interface must be implemented by all servlets. It defines the 
lifecycle methods: 
 init(ServletConfig config): Called by the servlet container to initialize the 
servlet. 
 service(ServletRequest req, ServletResponse res): Called by the servlet 
container to process a request. 
 destroy(): Called by the servlet container to indicate that the servlet is 
being taken out of service. 
Example: 
 
import javax.servlet.*; 
import java.io.IOException; 
 
public class MyServlet implements Servlet { 
public void init(ServletConfig config) throws ServletEx 
ception { 
// Initialization code 
} 
 
public void service(ServletRequest req, ServletResponse 
res) throws ServletException, IOException { 
res.setContentType("text/html"); 
res.getWriter().println("<h1>Hello, World!</h1>"); 
} 
 
public void destroy() { 
// Cleanup code 
} 

=== PAGE 27 ===
Servlet 24 
 
 
 
 
 
 
 
 
 
 
 
 
 
HttpServlet Class 
The class simplifies servlet development by providing methods to 
handle HTTP-specific services. 
 doGet(HttpServletRequest req, HttpServletResponse res): Handles GET 
requests. 
 doPost(HttpServletRequest req, HttpServletResponse res): Handles POST 
requests. 
Example: 
public ServletConfig getServletConfig() { 
return null; 
} 
public String getServletInfo() { 
return null; 
} 
} 
import javax.servlet.*; 
import javax.servlet.http.*; 
import java.io.IOException; 
public class HelloWorldServlet extends HttpServlet { 
protected void doGet(HttpServletRequest req, HttpServle 
tResponse res) throws ServletException, IOException { 
res.setContentType("text/html"); 
res.getWriter().println("<h1>Hello, World!</h1>"); 
} 
protected void doPost(HttpServletRequest req, HttpServl 
etResponse res) throws ServletException, IOException { 
// Handle POST request 
} 
} 

=== PAGE 28 ===
Servlet 25 
 
 
 
ServletConfig Interface 
The interface provides configuration information to a servlet. 
 
 getServletContext(): Returns the 
interaction with the servlet container. 
object, which allows 
 getInitParameter(String name): Returns a parameter value defined in the 
web.xml file. 
Example: 
 
 
 
 
 
 
 
ServletContext Interface 
The 
container. 
interface enables servlets to interact with the servlet 
 getInitParameter(String name): Returns a context-wide initialization 
parameter. 
 getAttribute(String name): Returns an attribute from the context. 
 setAttribute(String name, Object value): Sets an attribute in the context. 
 
Example: 
 
 
 
 
 
 
 
 
 
ServletRequest Interface 
public void init(ServletConfig config) throws ServletExcept 
ion { 
String paramValue = config.getInitParameter("myParam"); 
// Use the parameter value 
} 
public void init(ServletConfig config) throws ServletExcept 
ion { 
ServletContext context = config.getServletContext(); 
String paramValue = context.getInitParameter("contextPa 
ram"); 
context.setAttribute("myAttribute", "value"); 
} 
 
 

=== PAGE 29 ===
Servlet 26 
 
 
 
 
The interface provides methods to get information about the 
client's request. 
 getParameter(String name): Returns the value of a request parameter. 
 getAttribute(String name): Returns an attribute from the request. 
 
Example: 
 
 
 
 
 
 
 
ServletResponse Interface 
The 
client. 
interface provides methods to formulate a response to the 
 setContentType(String type): Sets the MIME type of the response. 
 
 getWriter(): Returns a 
 
Example: 
object to send text data to the client. 
 
 
 
 
 
 
 
 
HttpSession Interface 
The interface manages user sessions, allowing data to be stored 
and retrieved across multiple requests. 
 getAttribute(String name): Returns an attribute from the session. 
 setAttribute(String name, Object value): Sets an attribute in the session. 
 invalidate(): Invalidates the session. 
public void service(ServletRequest req, ServletResponse re 
s) throws ServletException, IOException { 
String paramValue = req.getParameter("paramName"); 
// Process the parameter value 
} 
public void service(ServletRequest req, ServletResponse re 
s) throws ServletException, IOException { 
res.setContentType("text/html"); 
PrintWriter out = res.getWriter(); 
out.println("<h1>Hello, World!</h1>"); 
} 
 
 

=== PAGE 30 ===
Servlet 27 
 
 
Example: 
 
 
 
 
 
 
 
 
 
 
6 . 3 Servlet Containers and Their Implementations 
A servlet container, also known as a servlet engine, is a part of a web server or 
application server that provides the network services over which requests and 
responses are sent. It also manages the lifecycle of servlets, mapping URLs to 
servlets, and ensuring that the servlet handles concurrent requests properly. 
Popular Servlet Containers: 
1. Apache Tomcat: An open-source implementation of the Java Servlet, 
JavaServer Pages, Java Expression Language, and Java WebSocket 
technologies. 
2. Jetty: A lightweight, highly scalable, and embeddable servlet container. 
3. GlassFish: An open-source application server project for the Java EE 
platform. 
4. JBoss/WildFly: An open-source Java EE-based application server. 
 
Example: Configuring Apache Tomcat 
1. Download and Install: 
 Follow the steps in Chapter 5 to download and install Apache Tomcat. 
2. Deploying a Servlet: 
 Package your web application as a WAR file and place it in the 
directory of Tomcat. 
 Alternatively, use the Tomcat Manager application to upload and deploy 
the WAR file. 
protected void doGet(HttpServletRequest req, HttpServletRes 
ponse res) throws ServletException, IOException { 
HttpSession session = req.getSession(); 
session.setAttribute("username", "JohnDoe"); 
String username = (String) session.getAttribute("userna 
me"); 
res.getWriter().println("Username: " + username); 
} 
 

=== PAGE 31 ===
Servlet 28 
 
 
 
 
 
 
3. Accessing the Servlet: 
 Start Tomcat and navigate to the deployed servletʼs URL, e.g., 
. 
Conclusion 
The Servlet API provides a powerful and flexible framework for building web 
applications in Java. Understanding the key interfaces and classes, such as 
, , , , , , 
and , is crucial for effective servlet development. Additionally, 
knowledge of servlet containers and their configurations ensures that your 
applications run smoothly and efficiently.  
This is the detailed outline and content for Chapter 6. Let me know if you need 
any modifications or additional information. 
 
Chapter 7: Servlet Example using web.xml 
7 . 1 Writing Servlets using Deployment Descriptors (web.xml) 
The file, also known as the deployment descriptor, is an XML file that 
defines how servlets are configured in a web application. It includes servlet 
definitions, servlet mappings, initialization parameters, and other configuration 
settings. 
Structure of web.xml 
The basic structure of a file is as follows: 
<web-app xmlns="<http://xmlns.jcp.org/xml/ns/javaee>" 
xmlns:xsi="<http://www.w3.org/2001/XMLSchema-insta 
nce>" 
xsi:schemaLocation="<http://xmlns.jcp.org/xml/ns/j 
avaee> 
<http://xmlns.jcp.org/xml/ns/j 
avaee/web-app_3_1.xsd>" 
version="3.1"> 
<!-- Servlet and mapping definitions go here --> 
</web-app> 
 
 
  
  
  
  

=== PAGE 32 ===
Servlet 29 
 
 
 
7 . 2 Configuring Servlet Mappings 
To define and map a servlet, you need to specify the servlet class and its URL 
pattern in the 
 
Example: 
file. 
Let's create a simple servlet named . 
1. Create the Servlet Class 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
1. Define the Servlet in web.xml 
package com.example; 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
public class HelloWorldServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
response.setContentType("text/html"); 
response.getWriter().println("<h1>Hello, World!</h1 
>"); 
} 
} 
<web-app xmlns="<http://xmlns.jcp.org/xml/ns/javaee>" 
xmlns:xsi="<http://www.w3.org/2001/XMLSchema-insta 
nce>" 
xsi:schemaLocation="<http://xmlns.jcp.org/xml/ns/j 
avaee> 
<http://xmlns.jcp.org/xml/ns/j 
avaee/web-app_3_1.xsd>" 
version="3.1"> 
 

=== PAGE 33 ===
Servlet 30 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
In this example: 
 : Defines the servlet and its fully qualified class name. 
: A unique name for the servlet. 
: The fully qualified class name of the servlet. 
: Maps a URL pattern to the servlet. 
: The URL pattern that will trigger the servlet. 
 
7 . 3 Handling Servlet Initialization Parameters 
Initialization parameters can be defined in the 
configuration values to a servlet. 
Example: 
file to provide 
1. Modify the Servlet Class to Use Initialization Parameters 
<servlet> 
<servlet-name>HelloWorldServlet</servlet-name> 
<servlet-class>com.example.HelloWorldServlet</servl 
et-class> 
</servlet> 
<servlet-mapping> 
<servlet-name>HelloWorldServlet</servlet-name> 
<url-pattern>/hello</url-pattern> 
</servlet-mapping> 
</web-app> 
package com.example; 
import java.io.IOException; 
import javax.servlet.ServletConfig; 
import javax.servlet.ServletException; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
<servlet-name> 
<servlet-class> 
<servlet-mapping> 
<url-pattern> 
 

=== PAGE 34 ===
Servlet 31 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
1. Define Initialization Parameters in web.xml 
 
<web-app xmlns="<http://xmlns.jcp.org/xml/ns/javaee>" 
xmlns:xsi="<http://www.w3.org/2001/XMLSchema-insta 
nce>" 
avaee> 
 
xsi:schemaLocation="<http://xmlns.jcp.org/xml/ns/j 
 
<http://xmlns.jcp.org/xml/ns/j 
avaee/web-app_3_1.xsd>" 
version="3.1"> 
 
<servlet> 
<servlet-name>HelloWorldServlet</servlet-name> 
<servlet-class>com.example.HelloWorldServlet</servl 
et-class> 
<init-param> 
<param-name>message</param-name> 
<param-value>Hello, World from init parameter! 
</param-value> 
</init-param> 
public class HelloWorldServlet extends HttpServlet { 
private String message; 
public void init(ServletConfig config) throws ServletEx 
ception { 
super.init(config); 
message = config.getInitParameter("message"); 
} 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
response.setContentType("text/html"); 
response.getWriter().println("<h1>" + message + "</ 
h1>"); 
} 
} 

=== PAGE 35 ===
Servlet 32 
 
 
<init-param> 
<param-name> 
 
 
 
 
 
 
 
 
 
 
 
 
In this example: 
 : Defines an initialization parameter for the servlet. 
 : The name of the parameter. 
: The value of the parameter. 
 
7 . 4 Example Application 
Let's put it all together and create a simple web application. 
1. Directory Structure 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
1. index.html 
</servlet> 
<servlet-mapping> 
<servlet-name>HelloWorldServlet</servlet-name> 
<url-pattern>/hello</url-pattern> 
</servlet-mapping> 
</web-app> 
webapps/ 
├── myapp/ 
│ 
│ 
│ 
│ 
│ 
│ 
│ 
├── WEB-INF/ 
│ 
│ 
│ 
│ 
│ 
├── web.xml 
└── classes/ 
└── com/ 
└── example/ 
└── HelloWorldServlet.class 
└── index.html 
<!DOCTYPE html> 
<html> 
<head> 
<title>My Servlet Application</title> 
</head> 
<body> 
<param-value> 

=== PAGE 36 ===
Servlet 33 
 
 
 
 
 
 
 
 
1. web.xml 
 
<web-app xmlns="<http://xmlns.jcp.org/xml/ns/javaee>" 
xmlns:xsi="<http://www.w3.org/2001/XMLSchema-insta 
nce>" 
avaee> 
 
xsi:schemaLocation="<http://xmlns.jcp.org/xml/ns/j 
 
<http://xmlns.jcp.org/xml/ns/j 
avaee/web-app_3_1.xsd>" 
version="3.1"> 
 
<servlet> 
<servlet-name>HelloWorldServlet</servlet-name> 
<servlet-class>com.example.HelloWorldServlet</servl 
et-class> 
<init-param> 
<param-name>message</param-name> 
<param-value>Hello, World from init parameter! 
</param-value> 
</init-param> 
</servlet> 
 
<servlet-mapping> 
<servlet-name>HelloWorldServlet</servlet-name> 
<url-pattern>/hello</url-pattern> 
</servlet-mapping> 
 
</web-app> 
 
1. HelloWorldServlet.java 
<h1>Welcome to My Servlet Application</h1> 
<p><a href="hello">Say Hello</a></p> 
</body> 
</html> 
package com.example; 

=== PAGE 37 ===
Servlet 34 
 
 
 
import java.io.IOException; 
import javax.servlet.ServletConfig; 
import javax.servlet.ServletException; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
public class HelloWorldServlet extends HttpServlet { 
private String message; 
 
public void init(ServletConfig config) throws ServletEx 
ception { 
super.init(config); 
message = config.getInitParameter("message"); 
} 
 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
response.setContentType("text/html"); 
response.getWriter().println("<h1>" + message + "</ 
h1>"); 
} 
} 
 
1. Compile and Deploy 
 Compile HelloWorldServlet.java and place the compiled class file in the 
directory. 
 
 Place in the directory. 
 
 Place 
 Deploy the 
installation. 
in the root of the 
directory to the 
directory. 
directory of your Tomcat 
 Start Tomcat and access . 
 
Conclusion 
 
 
 
WEB-INF 
 
  
 
 

=== PAGE 38 ===
Servlet 35 
 
 
 
 
 
Using for servlet configuration provides a flexible way to define 
servlets, their mappings, and initialization parameters. It allows for clear 
separation of configuration and code, making it easier to manage large web 
applications. By following the examples provided, you can create and deploy 
servlets effectively using deployment descriptors.  
 
Chapter 8: Servlet Example using 
Annotations 
8 . 1 Writing Servlets using Annotations 
Annotations provide a modern and more concise way to configure servlets, 
eliminating the need for extensive configuration. Introduced in Servlet 
3.0, annotations simplify servlet deployment by allowing configuration to be 
placed directly in the servlet class. 
Key Annotations: 
1. @WebServlet: Declares a servlet. 
2. @WebInitParam: Defines initialization parameters for a servlet. 
3. @WebFilter: Declares a filter. 
4. @WebListener: Declares a listener. 
 
8 . 2 Configuring Servlet Mappings using Annotations 
The annotation is used to declare a servlet and map it to a URL 
pattern. This eliminates the need to define the servlet and its mappings in 
. 
 
Example: 
Let's create a simple servlet named 
1. Create the Servlet Class 
using annotations. 
package com.example; 
import java.io.IOException; 
import javax.servlet.ServletException; 
 
 

=== PAGE 39 ===
Servlet 36 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
In this example: 
 
servlet and maps it to the 
 
 
 
URL pattern. 
 
 
: Declares the 
8 . 3 Handling Servlet Initialization Parameters using Annotations 
Initialization parameters can also be defined using the annotation, 
which can be specified within the 
 
Example: 
annotation. 
1. Modify the Servlet Class to Use Initialization Parameters 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
@WebServlet(name = "HelloWorldServlet", urlPatterns = {"/he 
llo"}) 
public class HelloWorldServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
response.setContentType("text/html"); 
response.getWriter().println("<h1>Hello, World!</h1 
>"); 
} 
} 
package com.example; 
import java.io.IOException; 
import javax.servlet.ServletConfig; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebInitParam; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
 
 
 

=== PAGE 40 ===
Servlet 37 
 
 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet( 
name = "HelloWorldServlet", 
urlPatterns = {"/hello"}, 
initParams = { 
@WebInitParam(name = "message", value = "Hello, Wor 
ld from init parameter!") 
} 
) 
public class HelloWorldServlet extends HttpServlet { 
private String message; 
 
public void init(ServletConfig config) throws ServletEx 
ception { 
super.init(config); 
message = config.getInitParameter("message"); 
} 
 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
response.setContentType("text/html"); 
response.getWriter().println("<h1>" + message + "</ 
h1>"); 
} 
} 
 
In this example: 
 
Defines an initialization parameter with the name 
corresponding value. 
8 . 4 Example Application 
 
 
: 
and a 
Let's put it all together and create a simple web application using annotations. 
1. Directory Structure 
 
 

=== PAGE 41 ===
Servlet 38 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
1. index.html 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
1. HelloWorldServlet.java 
webapps/ 
├── myapp/ 
│ 
│ 
│ 
│ 
│ 
│ 
├── WEB-INF/ 
│ 
│ 
│ 
│ 
└── classes/ 
└── com/ 
└── example/ 
└── HelloWorldServlet.class 
└── index.html 
<!DOCTYPE html> 
<html> 
<head> 
<title>My Servlet Application</title> 
</head> 
<body> 
<h1>Welcome to My Servlet Application</h1> 
<p><a href="hello">Say Hello</a></p> 
</body> 
</html> 
package com.example; 
import java.io.IOException; 
import javax.servlet.ServletConfig; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebInitParam; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
@WebServlet( 
name = "HelloWorldServlet", 

=== PAGE 42 ===
Servlet 39 
 
 
 
 
urlPatterns = {"/hello"}, 
initParams = { 
@WebInitParam(name = "message", value = "Hello, Wor 
ld from init parameter!") 
} 
) 
public class HelloWorldServlet extends HttpServlet { 
private String message; 
 
public void init(ServletConfig config) throws ServletEx 
ception { 
super.init(config); 
message = config.getInitParameter("message"); 
} 
 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
response.setContentType("text/html"); 
response.getWriter().println("<h1>" + message + "</ 
h1>"); 
} 
} 
 
1. Compile and Deploy 
 Compile HelloWorldServlet.java and place the compiled class file in the 
directory. 
 
 Place 
 Deploy the 
installation. 
in the root of the 
directory to the 
directory. 
directory of your Tomcat 
 Start Tomcat and access . 
 
Conclusion 
Using annotations for servlet configuration simplifies the process by 
embedding the configuration directly into the servlet class. This approach 
reduces the need for a separate file and provides a more streamlined 
 
 
 
  
 
 

=== PAGE 43 ===
Servlet 40 
 
 
development experience. By following the examples provided, you can create 
and deploy servlets effectively using annotations.  
This is the detailed outline and content for Chapter 8. Let me know if you need 
any modifications or additional information. 
 
Chapter 9: Servlet Life Cycle 
9 . 1 Understanding the Life Cycle of a Servlet 
The life cycle of a servlet is managed by the servlet container (e.g., Tomcat, 
Jetty). The life cycle consists of three main phases:  
1. Initialization: When the servlet is first loaded, the servlet container 
initializes it. 
2. Service: The servlet processes client requests. 
3. Destruction: The servlet is taken out of service and destroyed. 
These phases are controlled by specific methods in the 
interface and its subclasses. 
9 . 2 Initialization Phase 
The initialization phase occurs when the servlet is first created. The servlet 
container loads the servlet class and creates an instance of the servlet. The 
method is called to perform any required initialization. 
 
Key Method: 
: This method is called once when the servlet is 
instantiated. It is used to perform any servlet-specific initialization, such as 
setting up resources like database connections. 
@Override 
public void init(ServletConfig config) throws ServletExcept 
ion { 
super.init(config); 
// Perform initialization tasks 
System.out.println("Servlet is being initialized"); 
} 
 
 
init(ServletConfig config) 

=== PAGE 44 ===
Servlet 41 
 
 
 
 
 
  
9 . 3 Service Phase 
During the service phase, the servlet processes client requests. The servlet 
container calls the method each time a request is received. The 
method then delegates the request to the appropriate , , , 
, etc., methods based on the HTTP request method. 
 
Key Methods: 
: This method is 
called to process a request. It determines the HTTP method and calls the 
corresponding  method (e.g., , ). 
 
 
 
 
 
 
 
 
 
9 . 4 Destruction Phase 
The destruction phase occurs when the servlet is taken out of service. This can 
happen due to server shutdown or when the servlet container needs to reclaim 
resources. The 
 
Key Method: 
method is called to perform any cleanup operations. 
: This method is called once when the servlet is about to be 
destroyed. It is used to release resources that were acquired during the 
initialization or service phases. 
 
 
 
 
 
 
 
9 . 5 Servlet Life Cycle Example 
@Override 
protected void doGet(HttpServletRequest request, HttpServle 
tResponse response) 
throws ServletException, IOException { 
response.setContentType("text/html"); 
response.getWriter().println("<h1>Hello, World!</h1>"); 
} 
@Override 
public void destroy() { 
// Perform cleanup tasks 
System.out.println("Servlet is being destroyed"); 
} 
 
 
  
 
service(HttpServletRequest request, HttpServletResponse response) 
 
 

=== PAGE 45 ===
Servlet 42 
 
 
Let's create a simple servlet that demonstrates the servlet life cycle methods. 
1. Create the Servlet Class 
 
package com.example; 
 
import java.io.IOException; 
import javax.servlet.ServletConfig; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet(name = "LifeCycleServlet", urlPatterns = {"/lif 
ecycle"}) 
public class LifeCycleServlet extends HttpServlet { 
 
@Override 
public void init(ServletConfig config) throws ServletEx 
ception { 
super.init(config); 
System.out.println("Servlet is being initialized"); 
} 
 
@Override 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
response.setContentType("text/html"); 
response.getWriter().println("<h1>Hello, World!</h1 
>"); 
 
} 
 
System.out.println("Processing GET request"); 
 
@Override 
public void destroy() { 
System.out.println("Servlet is being destroyed"); 

=== PAGE 46 ===
Servlet 43 
 
 
 
 
 
 
 
1. Directory Structure 
 
 
 
 
 
 
 
 
 
 
 
 
1. index.html 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
1. Compile and Deploy 
 Compile LifeCycleServlet.java and place the compiled class file in the 
directory. 
 
 Place 
 Deploy the 
installation. 
in the root of the 
directory to the 
directory. 
directory of your Tomcat 
 Start Tomcat and access . 
 
Conclusion 
} 
} 
webapps/ 
├── myapp/ 
│ 
│ 
│ 
│ 
│ 
│ 
├── WEB-INF/ 
│ 
│ 
│ 
│ 
└── classes/ 
└── com/ 
└── example/ 
└── LifeCycleServlet.class 
└── index.html 
<!DOCTYPE html> 
<html> 
<head> 
<title>Servlet Life Cycle</title> 
</head> 
<body> 
<h1>Welcome to the Servlet Life Cycle Example</h1> 
<p><a href="lifecycle">Trigger Servlet</a></p> 
</body> 
</html> 
WEB- 
 
 
  
 
 

=== PAGE 47 ===
Servlet 44 
 
 
Understanding the servlet life cycle is crucial for managing resources 
effectively in a web application. The , , and methods 
provide hooks into the life cycle, allowing developers to initialize resources, 
process requests, and clean up resources, respectively. By following the 
example provided, you can see how these methods interact and manage a 
servlet's life cycle. 
This is the detailed outline and content for Chapter 9. Let me know if you need 
any modifications or additional information. 
 
Chapter 10: Servlet Parameters 
1 0 . 1 Handling Request Parameters in Servlets 
Request parameters are key-value pairs sent by the client to the server as part 
of an HTTP request. These parameters can be sent via: 
 Query strings in the URL for GET requests. 
 Form data in the body of POST requests. 
 
1 0 . 2 Retrieving Request Parameters 
To retrieve request parameters in a Servlet, you use methods provided by the 
object. 
Example: Retrieving parameters in a GET request 
Suppose you have a URL: 
Here is how you can retrieve these parameters in a Servlet: 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
@WebServlet("/example") 
public class ExampleServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
 
  
  
 
 

=== PAGE 48 ===
Servlet 45 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
Example: Retrieving parameters in a POST request 
When handling POST requests, parameters are typically sent as part of the 
request body. 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/example") 
public class ExampleServlet extends HttpServlet { 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) throws ServletException, IOExcepti 
on { 
// Retrieve parameters 
String name = request.getParameter("name"); 
String age = request.getParameter("age"); 
 
// Display parameters 
response.setContentType("text/html"); 
n { 
// Retrieve parameters 
String name = request.getParameter("name"); 
String age = request.getParameter("age"); 
// Display parameters 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Name: " + name + "<br 
>"); 
response.getWriter().println("Age: " + age + "<br 
>"); 
response.getWriter().println("</body></html>"); 
} 
} 

=== PAGE 49 ===
Servlet 46 
 
 
 
 
 
 
 
 
 
 
 
 
1 0 . 3 Dealing with Parameter Encoding and Decoding 
When dealing with parameters, especially in different languages or special 
characters, it's important to handle encoding properly. 
Example: Setting character encoding for parameters 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/example") 
public class ExampleServlet extends HttpServlet { 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) throws ServletException, IOExcepti 
on { 
// Set character encoding 
request.setCharacterEncoding("UTF-8"); 
 
// Retrieve parameters 
String name = request.getParameter("name"); 
 
 
 
 
8"); 
// Display parameters 
response.setContentType("text/html; charset=UTF- 
 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Name: " + name + "<br 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Name: " + name + "<br 
>"); 
response.getWriter().println("Age: " + age + "<br 
>"); 
response.getWriter().println("</body></html>"); 
} 
} 

=== PAGE 50 ===
Servlet 47 
 
 
 
 
 
 
 
 
1 0 . 4 Handling Multiple Parameters with the Same Name 
Sometimes, multiple parameters with the same name are sent in a request. You 
can handle these by using . 
Example: Retrieving multiple values 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/example") 
public class ExampleServlet extends HttpServlet { 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) throws ServletException, IOExcepti 
on { 
 
 
m"); 
// Retrieve multiple values 
String[] values = request.getParameterValues("ite 
 
// Display parameters 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
for (String value : values) { 
response.getWriter().println("Item: " + value + 
"<br>"); 
} 
response.getWriter().println("</body></html>"); 
} 
} 
>"); 
response.getWriter().println("</body></html>"); 
} 
} 

=== PAGE 51 ===
Servlet 48 
 
 
1 0 . 5 Example: Handling Form Submission 
Let's put everything together in an example that handles a form submission. 
HTML Form 
 
<!DOCTYPE html> 
<html> 
<head> 
<title>Form Submission</title> 
</head> 
<body> 
<form action="example" method="post"> 
Name: <input type="text" name="name"><br> 
Age: <input type="text" name="age"><br> 
Items: <input type="checkbox" name="item" value="it 
em1"> Item 1 
 
em2"> Item 2 
<input type="checkbox" name="item" value="it 
 
<input type="checkbox" name="item" value="it 
em3"> Item 3<br> 
<input type="submit" value="Submit"> 
</form> 
</body> 
</html> 
 
Servlet to handle the form 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
@WebServlet("/example") 
public class ExampleServlet extends HttpServlet { 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) throws ServletException, IOExcepti 
on { 

=== PAGE 52 ===
Servlet 49 
 
 
 
// Set character encoding 
request.setCharacterEncoding("UTF-8"); 
 
 
 
 
 
 
m"); 
// Retrieve parameters 
String name = request.getParameter("name"); 
String age = request.getParameter("age"); 
String[] items = request.getParameterValues("ite 
 
 
 
 
8"); 
 
 
>"); 
 
>"); 
 
 
 
 
 
>"); 
// Display parameters 
response.setContentType("text/html; charset=UTF- 
 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Name: " + name + "<br 
 
response.getWriter().println("Age: " + age + "<br 
 
response.getWriter().println("Items:<br>"); 
if (items != null) { 
for (String item : items) { 
response.getWriter().println(item + "<br 
 
 
 
 
<br>"); 
} 
} else { 
response.getWriter().println("No items selected 
} 
response.getWriter().println("</body></html>"); 
} 
} 
 
Chapter 11: Request Dispatcher 
1 1 . 1 Forwarding Requests Using RequestDispatcher 
The interface allows a request to be forwarded to another 
resource (such as a Servlet, HTML file, or JSP) within the same server. 

=== PAGE 53 ===
Servlet 50 
 
 
 
Example: Forwarding a Request 
Suppose we have a scenario where we want to forward a request from 
to . 
ServletA 
 
import java.io.IOException; 
import javax.servlet.RequestDispatcher; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/ServletA") 
public class ServletA extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
 
 
A"); 
// Set a request attribute 
request.setAttribute("message", "Hello from Servlet 
 
// Forward the request to ServletB 
RequestDispatcher dispatcher = request.getRequestDi 
spatcher("/ServletB"); 
dispatcher.forward(request, response); 
} 
} 
 
ServletB 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 

=== PAGE 54 ===
Servlet 51 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
In this example, when a request is made to , it sets a request attribute 
and forwards the request to 
displays it. 
. retrieves the attribute and 
1 1 . 2 Including Content from Other Resources 
The interface also allows a request to include content from 
another resource. This can be useful for creating modular web applications. 
Example: Including Content 
Suppose we want to include content from 
HeaderServlet 
in our main page. 
@WebServlet("/ServletB") 
public class ServletB extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Retrieve the request attribute 
String message = (String) request.getAttribute("mes 
sage"); 
// Display the message 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Message: " + messag 
e); 
response.getWriter().println("</body></html>"); 
} 
} 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
@WebServlet("/HeaderServlet") 
 
  
 

=== PAGE 55 ===
Servlet 52 
 
 
 
 
 
 
 
 
 
 
 
 
 
MainServlet 
 
import java.io.IOException; 
import javax.servlet.RequestDispatcher; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/MainServlet") 
public class MainServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
 
// Include header content 
RequestDispatcher dispatcher = request.getRequestDi 
spatcher("/HeaderServlet"); 
dispatcher.include(request, response); 
 
// Main content 
response.getWriter().println("<main><p>This is the 
main content of the page.</p></main>"); 
response.getWriter().println("</body></html>"); 
public class HeaderServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
response.getWriter().println("<header><h1>Welcome t 
o My Website</h1></header>"); 
} 
} 

=== PAGE 56 ===
Servlet 53 
 
 
 
 
 
 
 
 
 
In this example, when a request is made to , it includes the content 
from before displaying the main content. 
1 1 . 3 Redirecting Requests 
Unlike forwarding, redirection involves sending a new HTTP request to the 
client, instructing it to navigate to a different URL. This can be done using the 
method of the 
Example: Redirecting a Request 
object. 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
In this example, when a request is made to , it sends a redirect 
response to the client, instructing the browser to navigate to 
. 
 
1 1 . 4 Difference Between Forward and Redirect 
 Forward: The server internally forwards the request to another resource 
without the client being aware of it. The URL in the browser remains 
unchanged. 
} 
} 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
@WebServlet("/RedirectServlet") 
public class RedirectServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Redirect to another URL 
response.sendRedirect("<https://www.example.com>"); 
} 
} 
 
  
 

=== PAGE 57 ===
Servlet 54 
 
 
 
 Redirect: The server sends a response back to the client with a new URL, 
and the client makes a new request to this URL. The URL in the browser 
changes to the new URL. 
 
Feature Forward Redirect 
Browser URL Remains the same Changes to the new URL 
Request Scope Preserved Lost 
Performance Faster, as no additional 
request Slower, as it involves a new request 
Use Case Internal transfers within 
the app 
Navigating to an external site or different 
context within the app  
 
Chapter 12: Servlet Thread Models 
1 2 . 1 Understanding Single-Threaded vs. Multi-Threaded Models 
Servlets operate within a web container, which manages their lifecycle and 
handles client requests. The threading model used by a servlet impacts how it 
processes these requests. 
Single-Threaded Model: 
In a single-threaded model, a single thread processes all incoming requests, 
one at a time. This model simplifies development because it avoids 
concurrency issues but can become a bottleneck under heavy load. 
Example: Single-Threaded Servlet Implementation 
The interface can be used to indicate that a servlet should 
handle one request at a time. However, this interface is deprecated and not 
recommended for use. 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
public class SingleThreadedServlet extends HttpServlet impl 
ements SingleThreadModel { 
protected void doGet(HttpServletRequest request, HttpSe 

=== PAGE 58 ===
Servlet 55 
 
 
 
 
 
 
 
 
 
 
 
Note: The is deprecated because it does not scale well and 
can degrade performance. Modern servlets are typically multi-threaded. 
Multi-Threaded Model: 
In a multi-threaded model, multiple threads can handle multiple requests 
concurrently. This is the default and preferred approach for servlets. 
Example: Multi-Threaded Servlet Implementation 
Servlets by default are multi-threaded. Here is an example of a basic multi- 
threaded servlet: 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
1 2 . 2 Thread Safety Considerations in Servlet Programming 
In a multi-threaded environment, servlets must be thread-safe. Multiple threads 
can access the same servlet instance, which can lead to data inconsistency  
rvletResponse response) throws ServletException, IOExceptio 
n { 
response.getWriter().println("Handled by a single t 
hread at a time"); 
} 
} 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
@WebServlet("/MultiThreadedServlet") 
public class MultiThreadedServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
response.getWriter().println("Handled by multiple t 
hreads concurrently"); 
} 
} 

=== PAGE 59 ===
Servlet 56 
 
 
 
and unpredictable behavior if proper synchronization is not applied. 
Example: Non-Thread-Safe Servlet 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/NonThreadSafeServlet") 
public class NonThreadSafeServlet extends HttpServlet { 
private int counter = 0; 
 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
counter++; 
response.getWriter().println("Counter value: " + co 
unter); 
} 
} 
 
In the above example, the variable is shared by all threads, leading to 
race conditions and incorrect counter values. 
Making a Servlet Thread-Safe 
To make a servlet thread-safe, you can: 
 Avoid using instance variables to store request-specific data. 
 Use synchronization to control access to shared resources. 
 Use thread-local storage. 
Example: Using Local Variables 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 

=== PAGE 60 ===
Servlet 57 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
Example: Using Synchronization 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/SynchronizedServlet") 
public class SynchronizedServlet extends HttpServlet { 
private int counter = 0; 
 
protected synchronized void doGet(HttpServletRequest re 
quest, HttpServletResponse response) throws ServletExceptio 
n, IOException { 
counter++; 
response.getWriter().println("Counter value: " + co 
unter); 
} 
} 
 
Example: Using Thread-Local Storage 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
@WebServlet("/ThreadSafeServlet") 
public class ThreadSafeServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
int counter = 0; // Local variable, thread-safe 
counter++; 
response.getWriter().println("Counter value: " + co 
unter); 
} 
} 

=== PAGE 61 ===
Servlet 58 
 
 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/ThreadLocalServlet") 
public class ThreadLocalServlet extends HttpServlet { 
private static final ThreadLocal<Integer> threadLocalCo 
unter = new ThreadLocal<Integer>() { 
@Override 
protected Integer initialValue() { 
return 0; 
} 
}; 
 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
 
 
 
 
 
unter); 
} 
int counter = threadLocalCounter.get(); 
counter++; 
threadLocalCounter.set(counter); 
response.getWriter().println("Counter value: " + co 
} 
 
In this example, ensures each thread has its own instance of the 
variable, avoiding thread-safety issues. 
 
1 2 . 3 Best Practices for Writing Thread-Safe Servlets 
1. Avoid Instance Variables for Shared Data: 
 Use local variables within methods to store data that should not be 
shared between threads. 
2. Use Synchronization Wisely: 
 

=== PAGE 62 ===
Servlet 59 
 
 
 
 
 Synchronize only the critical sections of code that modify shared 
resources. 
 Minimize the scope of synchronization to avoid performance 
bottlenecks. 
3. Use Thread-Local Storage: 
 Utilize for variables that need to be unique per thread. 
4. Immutable Objects: 
 Prefer immutable objects for shared data, as they are inherently thread- 
safe. 
5. Stateless Servlets: 
 Design servlets to be stateless whenever possible, meaning they do not 
rely on instance variables to maintain state. 
6. Concurrent Collections: 
 
 Use Java's concurrent collections ( 
etc.) for shared data structures. 
Example: Using ConcurrentHashMap 
, , 
import java.io.IOException; 
import java.util.concurrent.ConcurrentHashMap; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
@WebServlet("/ConcurrentServlet") 
public class ConcurrentServlet extends HttpServlet { 
private ConcurrentHashMap<String, Integer> userAccessCo 
unt = new ConcurrentHashMap<>(); 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
String user = request.getParameter("user"); 
 

=== PAGE 63 ===
Servlet 60 
 
 
 
 
 
 
 
 
 
 
In this example, 
access count data. 
ensures thread-safe access to the user 
This chapter has provided an overview of servlet thread models, highlighting 
the differences between single-threaded and multi-threaded models, along 
with best practices for writing thread-safe servlets. By following these 
guidelines, you can ensure your servlets perform efficiently and correctly in a 
concurrent environment. 
 
Chapter 13: HttpServletRequest & 
HttpServletResponse 
1 3 . 1 Understanding HttpServletRequest and 
HttpServletResponse Objects 
In the Servlet API, the and objects 
represent the request and response of an HTTP interaction. These objects 
provide methods to access request parameters, headers, cookies, and content, 
and to manipulate the response sent back to the client. 
1 3 . 2 HttpServletRequest 
The interface extends and provides additional 
methods to handle HTTP-specific data. 
Common Methods in HttpServletRequest: 
1. Request Parameters: 
 
 
 
as a 
 
, or 
: Returns the value of a request parameter 
if the parameter does not exist. 
: Returns an array of 
containing all values of the specified request parameter. 
objects 
userAccessCount.merge(user, 1, Integer::sum); 
response.getWriter().println("Access count for " + 
user + ": " + userAccessCount.get(user)); 
} 
} 
 
 
  
 
  
String getParameter(String name) 
 
 
String[] getParameterValues(String name) 
  

=== PAGE 64 ===
Servlet 61 
 
 
 
 
 
: Returns an of 
objects containing the names of the request parameters. 
2. Request Attributes: 
 
 
 
as an 
 
attribute. 
 
, or 
: Returns the value of the named attribute 
if no attribute of the given name exists. 
: Sets the value of the named 
 
3. Request Headers: 
 
header as a . 
 
 
: Returns the value of the specified request 
 
 
 
header names. 
4. Session Management: 
: Returns an of all the 
: Returns the current session associated with this 
request, or creates one if it does not exist. 
: Returns the current session if it 
exists, or if is . 
5. Request Information: 
: Returns the HTTP method (e.g., GET, POST) with 
which the request was made. 
: Returns the part of this request's URL from the 
protocol name up to the query string. 
: Returns the query string that is contained in the 
request URL after the path. 
Example: Retrieving Request Information 
import java.io.IOException; 
import java.util.Enumeration; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
Enumeration<String> getParameterNames() 
  
Object getAttribute(String name) 
 
  
 
 
Enumeration<String> getHeaderNames() 
  
 
HttpSession getSession(boolean create) 
 
  
 
 
 

=== PAGE 65 ===
Servlet 62 
 
 
 
@WebServlet("/RequestInfoServlet") 
public class RequestInfoServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
 
// Request parameters 
String name = request.getParameter("name"); 
response.getWriter().println("Parameter 'name': " + 
name + "<br>"); 
 
 
 
 
e"); 
// Request attributes 
Object attribute = request.getAttribute("attribut 
 
response.getWriter().println("Attribute 'attribut 
e': " + attribute + "<br>"); 
 
// Request headers 
Enumeration<String> headerNames = request.getHeader 
Names(); 
while (headerNames.hasMoreElements()) { 
String headerName = headerNames.nextElement(); 
String headerValue = request.getHeader(headerNa 
me); 
response.getWriter().println("Header '" + heade 
rName + "': " + headerValue + "<br>"); 
} 
 
// Request information 
response.getWriter().println("HTTP Method: " + requ 
est.getMethod() + "<br>"); 
response.getWriter().println("Request URI: " + requ 
est.getRequestURI() + "<br>"); 
response.getWriter().println("Query String: " + req 
uest.getQueryString() + "<br>"); 

=== PAGE 66 ===
Servlet 63 
 
 
 
 
 
 
 
1 3 . 3 HttpServletResponse 
The interface extends and provides methods 
to handle HTTP-specific functionality. 
Common Methods in HttpServletResponse: 
1. Setting Response Headers: 
 
  
given name and value. 
 
  
given name and value. 
2. Setting Status Codes: 
 
 
 
 
 
: Sets a response header with the 
 
 
: Adds a response header with the 
: Sets the status code for this response. 
: Sends an error response to the client 
using the specified status code and message.  
3. Redirecting: 
: Sends a temporary redirect response to 
the client using the specified redirect location URL. 
4. Writing Response Content: 
 
: Returns a 
character text to the client. 
object that can send 
: Returns a 
that can send binary data to the client. 
Example: Setting Response Headers and Status 
object 
 
 
response.getWriter().println("</body></html>"); 
} 
} 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
 
  
 
 
 
 
  
ServletOutputStream getOutputStream() 
  

=== PAGE 67 ===
Servlet 64 
 
 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/ResponseHeaderServlet") 
public class ResponseHeaderServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Setting response headers 
response.setHeader("Content-Type", "text/html"); 
response.addHeader("Custom-Header", "CustomValue"); 
 
// Setting status code 
response.setStatus(HttpServletResponse.SC_OK); 
 
// Writing response content 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Response headers and 
status set."); 
response.getWriter().println("</body></html>"); 
} 
} 
 
1 3 . 4 Manipulating Request and Response Headers 
Request and response headers are crucial for controlling the behavior of HTTP 
interactions. They provide metadata about the request and response, such as 
content type, encoding, caching policies, and more.  
Example: Manipulating Headers 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
@WebServlet("/HeaderManipulationServlet") 

=== PAGE 68 ===
Servlet 65 
 
 
public class HeaderManipulationServlet extends HttpServlet 
{ 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Setting request headers (not usually done in a s 
ervlet, shown for completeness) 
request.setAttribute("Custom-Request-Header", "Requ 
estValue"); 
 
// Setting response headers 
response.setHeader("Content-Type", "text/html"); 
response.addHeader("Cache-Control", "no-cache, no-s 
tore, must-revalidate"); 
response.addHeader("Pragma", "no-cache"); 
response.addHeader("Expires", "0"); 
 
// Writing response content 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Response headers mani 
pulated."); 
response.getWriter().println("</body></html>"); 
} 
} 
 
1 3 . 5 Accessing Request Attributes and Session Information 
Request attributes allow data to be shared between different components 
handling the same request. Sessions provide a way to maintain state across 
multiple requests from the same client. 
Example: Accessing Request Attributes and Session Information 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 

=== PAGE 69 ===
Servlet 66 
 
 
 
import javax.servlet.http.HttpSession; 
 
@WebServlet("/SessionServlet") 
public class SessionServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Setting request attributes 
request.setAttribute("requestAttribute", "RequestVa 
lue"); 
 
 
 
 
 
lue"); 
// Accessing session information 
HttpSession session = request.getSession(); 
session.setAttribute("sessionAttribute", "SessionVa 
 
// Writing response content 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Request Attribute: " 
+ request.getAttribute("requestAttribute") + "<br>"); 
response.getWriter().println("Session Attribute: " 
+ session.getAttribute("sessionAttribute") + "<br>"); 
response.getWriter().println("Session ID: " + sessi 
on.getId() + "<br>"); 
response.getWriter().println("</body></html>"); 
} 
} 
 
This chapter provides an in-depth understanding of and 
objects, their methods, and how to manipulate headers and 
manage sessions effectively in your web applications.  
 
Chapter 14: Session Management 
1 4 . 1 Exploring HttpSession for Session Management 
 

=== PAGE 70 ===
Servlet 67 
 
 
 
HttpSession is an interface in the Servlet API used for session management. A 
session is a way to identify a user across multiple requests and maintain state. 
Creating and Accessing a Session 
The 
sessions. 
 
interface provides methods to create and access 
 
 
: Returns the current session associated with this 
request or, if there is no current session, creates one. 
: Returns the current session if it exists, 
or creates one if the parameter is . If is , it returns 
the current session or if there is no current session. 
Example: Creating and Accessing a Session 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
import javax.servlet.http.HttpSession; 
 
@WebServlet("/SessionExample") 
public class SessionExample extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Create or get the current session 
HttpSession session = request.getSession(); 
 
// Set session attribute 
session.setAttribute("username", "john_doe"); 
 
// Get session attribute 
String username = (String) session.getAttribute("us 
ername"); 
 
// Display session information 
 
 
HttpSession getSession(boolean create) 
 
  
  
  

=== PAGE 71 ===
Servlet 68 
 
 
 
 
 
 
 
 
 
 
 
 
 
1 4 . 2 Handling Session Attributes 
Session attributes are used to store data specific to a user session. They are 
stored on the server side and can be accessed across multiple requests. 
Common Methods for Handling Session Attributes: 
: Binds an object to this session, 
using the name specified. 
 
name in this session. 
 
 
: Returns the object bound with the specified 
 
 
: Removes the object bound with the 
specified name from this session. 
Example: Handling Session Attributes 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Username: " + usernam 
e + "<br>"); 
response.getWriter().println("Session ID: " + sessi 
on.getId() + "<br>"); 
response.getWriter().println("</body></html>"); 
} 
} 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
import javax.servlet.http.HttpSession; 
@WebServlet("/SessionAttributeExample") 
public class SessionAttributeExample extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Create or get the current session 
 
Object getAttribute(String name) 
void removeAttribute(String name) 

=== PAGE 72 ===
Servlet 69 
 
 
HttpSession session = request.getSession(); 
 
 
 
 
 
m"); 
// Set session attributes 
session.setAttribute("userId", "12345"); 
session.setAttribute("email", "john.doe@example.co 
 
 
 
 
Id"); 
 
l"); 
// Get session attributes 
String userId = (String) session.getAttribute("user 
String email = (String) session.getAttribute("emai 
 
// Remove session attribute 
session.removeAttribute("email"); 
 
// Display session information 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
response.getWriter().println("User ID: " + userId + 
"<br>"); 
response.getWriter().println("Email: " + email + "< 
br>"); 
response.getWriter().println("Session ID: " + sessi 
on.getId() + "<br>"); 
response.getWriter().println("</body></html>"); 
} 
} 
 
1 4 . 3 Session Tracking Techniques 
Session tracking involves maintaining a user's state across multiple requests. 
Common techniques for session tracking include cookies, URL rewriting, and 
hidden form fields. 
Cookies: 
Cookies are small pieces of data stored on the client side and sent to the server 
with each request. 
Example: Using Cookies for Session Tracking 

=== PAGE 73 ===
Servlet 70 
 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.Cookie; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/CookieExample") 
public class CookieExample extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Create a new cookie 
Cookie cookie = new Cookie("sessionId", "abcdef"); 
cookie.setMaxAge(60 * 60); // 1 hour 
response.addCookie(cookie); 
 
// Retrieve cookies 
Cookie[] cookies = request.getCookies(); 
if (cookies != null) { 
for (Cookie c : cookies) { 
response.getWriter().println("Cookie: " + 
c.getName() + "=" + c.getValue() + "<br>"); 
} 
} 
 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Cookie added."); 
response.getWriter().println("</body></html>"); 
} 
} 
 
URL Rewriting: 
URL rewriting involves appending session information to the URL. 
Example: Using URL Rewriting for Session Tracking 

=== PAGE 74 ===
Servlet 71 
 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
import javax.servlet.http.HttpSession; 
 
@WebServlet("/UrlRewriteExample") 
public class UrlRewriteExample extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Create or get the current session 
HttpSession session = request.getSession(); 
session.setAttribute("username", "john_doe"); 
 
// URL rewriting 
String url = response.encodeURL("nextPage.jsp"); 
 
// Display URL with session ID 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
response.getWriter().println("URL with session ID: 
<a href='" + url + "'>Next Page</a>"); 
response.getWriter().println("</body></html>"); 
} 
} 
 
Hidden Form Fields: 
Hidden form fields involve embedding session information in HTML forms. 
Example: Using Hidden Form Fields for Session Tracking 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 

=== PAGE 75 ===
Servlet 72 
 
 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
import javax.servlet.http.HttpSession; 
 
@WebServlet("/HiddenFieldExample") 
public class HiddenFieldExample extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Create or get the current session 
HttpSession session = request.getSession(); 
session.setAttribute("username", "john_doe"); 
 
// Display form with hidden field 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
response.getWriter().println("<form action='nextPag 
e.jsp' method='post'>"); 
response.getWriter().println("<input type='hidden' 
name='sessionId' value='" + session.getId() + "'>"); 
response.getWriter().println("<input type='submit' 
value='Submit'>"); 
response.getWriter().println("</form>"); 
response.getWriter().println("</body></html>"); 
} 
} 
 
1 4 . 4 Session Timeout and Invalidating Sessions 
Sessions have a default timeout period after which they are invalidated. You 
can configure the timeout period or manually invalidate a session. 
Setting Session Timeout: 
import javax.servlet.http.HttpSession; 
HttpSession session = request.getSession(); 
session.setMaxInactiveInterval(30 * 60); // 30 minutes 

=== PAGE 76 ===
Servlet 73 
 
 
Invalidating a Session: 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
import javax.servlet.http.HttpSession; 
 
@WebServlet("/InvalidateSessionExample") 
public class InvalidateSessionExample extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Create or get the current session 
HttpSession session = request.getSession(); 
 
// Invalidate the session 
session.invalidate(); 
 
 
 
 
 
d."); 
 
} 
} 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Session invalidate 
 
response.getWriter().println("</body></html>"); 
 
1 4 . 5 Best Practices for Session Management 
1. Use HTTPS: 
 Ensure session data is transmitted over HTTPS to protect against 
interception. 
2. Set Secure and HttpOnly Flags: 
 
 Set the 
security. 
and flags on session cookies to enhance 
 
  

=== PAGE 77 ===
Servlet 74 
 
 
3. Regenerate Session ID: 
 Regenerate the session ID upon user authentication to prevent session 
fixation attacks. 
4. Limit Session Lifetime: 
 Set appropriate session timeout values to limit the lifetime of sessions. 
5. Avoid Storing Sensitive Data: 
 Avoid storing sensitive data in session attributes. 
6. Use Strong Session IDs: 
 Use strong, randomly generated session IDs to prevent session 
hijacking. 
Example: Best Practices Implementation 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.Cookie; 
import javax.servlet.http.HttpServlet; 
import 
 
javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
import javax.servlet.http.HttpSession; 
 
@WebServlet("/SessionBestPracticesExample") 
public class SessionBestPracticesExample extends HttpServle 
t { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Create or get the current session 
HttpSession session = request.getSession(); 
 
 
 
 
tes 
// Set session timeout 
session.setMaxInactiveInterval(30 * 60); // 30 minu 

=== PAGE 78 ===
Servlet 75 
 
 
 
// Set Secure and HttpOnly flags on session cookie 
Cookie sessionCookie = new Cookie("JSESSIONID", ses 
sion.getId()); 
sessionCookie.setSecure(true); 
sessionCookie.setHttpOnly(true); 
response.addCookie(sessionCookie); 
 
 
 
 
{ 
 
e); 
e"); 
// Regenerate session ID upon user authentication 
if ("login".equals(request.getParameter("action"))) 
 
HttpSession newSession = request.getSession(tru 
newSession.setAttribute("username", "john_do 
} 
 
response.setContentType("text/html"); 
response.getWriter().println("<html><body>"); 
response.getWriter().println("Session best practice 
s implemented."); 
response.getWriter().println("</body></html>"); 
} 
} 
 
Chapter 15: Servlet Scopes 
1 5 . 1 Understanding Different Scopes in Servlets 
Servlets support various scopes that define the lifecycle and visibility of data. 
The three primary scopes are: 
1. Request Scope 
2. Session Scope 
3. Application Scope 
Each scope serves a different purpose and is used to manage data across 
different components of a web application.  

=== PAGE 79 ===
Servlet 76 
 
 
1 5 . 2 Request Scope 
Request Scope is used to store data that is accessible only during a single 
HTTP request. Once the server processes the request and sends the response, 
the data in this scope is discarded. 
Setting Request Attributes: 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/RequestScopeServlet") 
public class RequestScopeServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Setting a request attribute 
request.setAttribute("message", "Hello from Request 
Scope!"); 
 
// Forwarding to another servlet or JSP 
request.getRequestDispatcher("/displayRequestScope. 
jsp").forward(request, response); 
} 
} 
 
Retrieving Request Attributes: 
// displayRequestScope.jsp 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 
<body> 
<h2>Request Scope Example</h2> 
<p>Message: ${requestScope.message}</p> 

=== PAGE 80 ===
Servlet 77 
 
 
 
 
 
1 5 . 3 Session Scope 
Session Scope is used to store data that is accessible across multiple requests 
from the same client. This scope is ideal for maintaining user state and data 
during a user session. 
Setting Session Attributes: 
 
import java.io.IOException; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
import javax.servlet.http.HttpSession; 
 
@WebServlet("/SessionScopeServlet") 
public class SessionScopeServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
HttpSession session = request.getSession(); 
 
// Setting a session attribute 
session.setAttribute("username", "john_doe"); 
 
// Redirecting to another servlet or JSP 
response.sendRedirect("displaySessionScope.jsp"); 
} 
} 
 
Retrieving Session Attributes: 
</body> 
</html> 
// displaySessionScope.jsp 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 

=== PAGE 81 ===
Servlet 78 
 
 
 
 
 
 
 
 
 
1 5 . 4 Application Scope 
Application Scope is used to store data that is accessible to all servlets and 
JSPs in the web application. This scope is ideal for sharing data across the 
entire application. 
Setting Application Attributes: 
 
import java.io.IOException; 
import javax.servlet.ServletContext; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
 
@WebServlet("/ApplicationScopeServlet") 
public class ApplicationScopeServlet extends HttpServlet { 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
ServletContext context = getServletContext(); 
 
// Setting an application attribute 
context.setAttribute("globalMessage", "Hello from A 
pplication Scope!"); 
 
// Forwarding to another servlet or JSP 
request.getRequestDispatcher("/displayApplicationSc 
ope.jsp").forward(request, response); 
} 
} 
<html> 
<body> 
<h2>Session Scope Example</h2> 
<p>Username: ${sessionScope.username}</p> 
</body> 
</html> 

=== PAGE 82 ===
Servlet 79 
 
 
Retrieving Application Attributes: 
 
 
 
 
 
 
 
 
 
 
 
 
1 5 . 5 Managing Data Across Different Scopes 
Each scope has a specific purpose and lifespan, and choosing the appropriate 
scope depends on how long you need to retain data and where it needs to be 
accessible. 
 Request Scope: Use this for temporary data that is only needed during a 
single request. 
 Session Scope: Use this for user-specific data that should persist across 
multiple requests in a single session. 
 Application Scope: Use this for data that should be accessible across the 
entire application and by all users. 
Example: Combining Scopes 
// displayApplicationScope.jsp 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 
<body> 
<h2>Application Scope Example</h2> 
<p>Global Message: ${applicationScope.globalMessage}</p 
> 
</body> 
</html> 
import java.io.IOException; 
import javax.servlet.ServletContext; 
import javax.servlet.ServletException; 
import javax.servlet.annotation.WebServlet; 
import javax.servlet.http.HttpServlet; 
import javax.servlet.http.HttpServletRequest; 
import javax.servlet.http.HttpServletResponse; 
import javax.servlet.http.HttpSession; 
@WebServlet("/CombinedScopesServlet") 
public class CombinedScopesServlet extends HttpServlet { 

=== PAGE 83 ===
Servlet 80 
 
 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) throws ServletException, IOExceptio 
n { 
// Request Scope 
request.setAttribute("requestMessage", "Request Sco 
pe Message"); 
 
// Session Scope 
HttpSession session = request.getSession(); 
session.setAttribute("sessionMessage", "Session Sco 
pe Message"); 
 
// Application Scope 
ServletContext context = getServletContext(); 
context.setAttribute("applicationMessage", "Applica 
tion Scope Message"); 
 
// Forwarding to JSP to display all scopes 
request.getRequestDispatcher("/displayCombinedScope 
s.jsp").forward(request, response); 
} 
} 
 
Displaying Data from Different Scopes 
// displayCombinedScopes.jsp 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 
<body> 
<h2>Combined Scopes Example</h2> 
<p>Request Message: ${requestScope.requestMessage}</p> 
<p>Session Message: ${sessionScope.sessionMessage}</p> 
<p>Application Message: ${applicationScope.applicationM 
essage}</p> 
</body> 
</html> 

=== PAGE 84 ===
Servlet 81 
 
 
1 5 . 6 Summary 
In this chapter, we've explored the different scopes available in Servlets: 
 Request Scope for data that is needed only during a single request. 
 Session Scope for user-specific data that persists across multiple requests 
in a session. 
 Application Scope for data shared across the entire application. 
 
Chapter 16: Servlet Filter & Filter Chaining 
1 6 . 1 Introduction to Servlet Filters 
Servlet Filters are objects that perform filtering tasks on either the request to a 
resource or on the response from a resource, or both. Filters are pluggable and 
reusable components that provide an elegant way to manipulate request and 
response objects before they reach the target resource or after the resource 
has processed the request. 
Common uses of filters include: 
 Logging request and response data 
 Authentication and authorization 
 Input validation 
 Compression and decompression 
 Data encryption and decryption 
 
1 6 . 2 Implementing and Configuring Servlet Filters 
To create a filter, you need to implement the 
which includes three methods: 
interface, 
 
 
filter. 
 
: Called by the web container to initialize the 
 
 
: 
Called each time a request/response pair is passed through the filter chain. 
This is where the filtering logic is applied. 
: Called by the web container to indicate to the filter that it is being 
taken out of service. 
 
init(FilterConfig filterConfig) 
doFilter(ServletRequest request, ServletResponse response, FilterChain chain) 
 

=== PAGE 85 ===
Servlet 82 
 
 
Example: Creating a Simple Logging Filter 
 
import java.io.IOException; 
import javax.servlet.Filter; 
import javax.servlet.FilterChain; 
import javax.servlet.FilterConfig; 
import javax.servlet.ServletException; 
import javax.servlet.ServletRequest; 
import javax.servlet.ServletResponse; 
import javax.servlet.annotation.WebFilter; 
 
@WebFilter("/exampleServlet") 
public class LoggingFilter implements Filter { 
 
public void init(FilterConfig filterConfig) throws Serv 
letException { 
// Initialization code, if needed 
} 
 
public void doFilter(ServletRequest request, ServletRes 
ponse response, FilterChain chain) 
throws IOException, ServletException { 
// Log the request 
System.out.println("Request received at " + new jav 
a.util.Date()); 
 
// Pass the request and response to the next filter 
or target resource 
chain.doFilter(request, response); 
 
// Log the response 
System.out.println("Response generated at " + new j 
ava.util.Date()); 
} 
 
public void destroy() { 
// Cleanup code, if needed 

=== PAGE 86 ===
Servlet 83 
 
 
 
 
 
 
Configuring the Filter Using Annotations 
 
 
 
 
 
 
 
Configuring the Filter in web.xml 
 
 
 
 
 
 
 
 
 
 
1 6 . 3 Filter Chaining 
Filters can be configured to form a filter chain. The order in which filters are 
defined in the 
they are applied. 
or through annotations determines the order in which 
Example: Multiple Filters in a Chain 
} 
} 
@WebFilter("/exampleServlet") 
public class LoggingFilter implements Filter { 
// Implementation as shown above 
} 
<filter> 
<filter-name>LoggingFilter</filter-name> 
<filter-class>com.example.LoggingFilter</filter-class> 
</filter> 
<filter-mapping> 
<filter-name>LoggingFilter</filter-name> 
<url-pattern>/exampleServlet</url-pattern> 
</filter-mapping> 
@WebFilter("/exampleServlet") 
public class AuthenticationFilter implements Filter { 
public void doFilter(ServletRequest request, ServletRes 
ponse response, FilterChain chain) 
throws IOException, ServletException { 
// Authentication logic 
System.out.println("Authentication Filter execute 
d"); 
chain.doFilter(request, response); 
} 
 

=== PAGE 87 ===
Servlet 84 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
Configuring the Filter Chain in web.xml 
 
<filter> 
<filter-name>AuthenticationFilter</filter-name> 
<filter-class>com.example.AuthenticationFilter</filter- 
class> 
</filter> 
<filter> 
<filter-name>AuthorizationFilter</filter-name> 
<filter-class>com.example.AuthorizationFilter</filter-c 
lass> 
</filter> 
 
<filter-mapping> 
<filter-name>AuthenticationFilter</filter-name> 
<url-pattern>/exampleServlet</url-pattern> 
</filter-mapping> 
<filter-mapping> 
<filter-name>AuthorizationFilter</filter-name> 
<url-pattern>/exampleServlet</url-pattern> 
</filter-mapping> 
 
In this configuration, the 
. 
is applied first, followed by the 
} 
@WebFilter("/exampleServlet") 
public class AuthorizationFilter implements Filter { 
public void doFilter(ServletRequest request, ServletRes 
ponse response, FilterChain chain) 
throws IOException, ServletException { 
// Authorization logic 
System.out.println("Authorization Filter execute 
d"); 
chain.doFilter(request, response); 
} 
} 
 
 

=== PAGE 88 ===
Servlet 85 
 
 
1 6 . 4 Practical Use Cases 
Logging Filter Example 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
Compression Filter Example 
 
@WebFilter("/compress/*") 
public class CompressionFilter implements Filter { 
public void doFilter(ServletRequest request, ServletRes 
ponse response, FilterChain chain) 
throws IOException, ServletException { 
HttpServletResponse httpResponse = (HttpServletResp 
onse) response; 
httpResponse.setHeader("Content-Encoding", "gzip"); 
GZIPOutputStream gzipOutputStream = new GZIPOutputS 
tream(httpResponse.getOutputStream()); 
chain.doFilter(request, new HttpServletResponseWrap 
per(httpResponse) { 
public ServletOutputStream getOutputStream() { 
return new ServletOutputStream() { 
public void write(int b) throws IOExcep 
tion { 
gzipOutputStream.write(b); 
} 
}; 
} 
 
@WebFilter("/*") 
public class LoggingFilter implements Filter { 
public void doFilter(ServletRequest request, ServletRes 
ponse response, FilterChain chain) 
throws IOException, ServletException { 
HttpServletRequest httpRequest = (HttpServletReques 
t) request; 
System.out.println("Request URL: " + httpRequest.ge 
tRequestURL().toString()); 
chain.doFilter(request, response); 
} 
} 

=== PAGE 89 ===
Servlet 86 
 
 
 
 
 
 
 
1 6 . 5 Summary 
Servlet Filters are powerful tools for preprocessing and postprocessing 
requests and responses in a web application. They provide a modular way to 
implement common tasks such as logging, authentication, authorization, input 
validation, and more. By understanding how to implement and configure filters, 
and how to manage filter chains, developers can create robust and 
maintainable web applications. 
 
Chapter 17: Listeners 
1 7 . 1 Understanding Servlet Listeners 
Servlet listeners are special classes in Java EE that allow you to listen to and 
respond to various events in a web application lifecycle. These events include 
changes to the servlet context, HTTP session lifecycle, and servlet request 
lifecycle. Listeners provide a way to handle asynchronous events and are used 
for tasks such as: 
 Initializing resources when the application starts. 
 Cleaning up resources when the application shuts down. 
 Monitoring session creation and destruction. 
 Tracking user activities. 
 
1 7 . 2 Types of Servlet Listeners 
There are several types of listeners in the servlet API, each corresponding to 
different lifecycle events: 
1. ServletContextListener 
2. HttpSessionListener 
3. ServletRequestListener 
4. Other specialized listeners 
}); 
gzipOutputStream.close(); 
} 
} 

=== PAGE 90 ===
Servlet 87 
 
 
 
 
1 7 . 3 Implementing ServletContextListener 
The interface is used to receive notifications about 
changes to the servlet context of the web application. This is useful for 
performing tasks such as initializing resources or performing cleanup 
operations when the application is started or stopped. 
Example: ServletContextListener 
 
import javax.servlet.ServletContextEvent; 
import javax.servlet.ServletContextListener; 
import javax.servlet.annotation.WebListener; 
 
@WebListener 
public class MyAppServletContextListener implements Servlet 
ContextListener { 
 
@Override 
public void contextInitialized(ServletContextEvent sce) 
{ 
// Code to initialize resources when the applicatio 
n starts 
System.out.println("Application started. Initializi 
ng resources..."); 
} 
 
@Override 
public void contextDestroyed(ServletContextEvent sce) { 
// Code to cleanup resources when the application s 
tops 
System.out.println("Application stopped. Cleaning u 
p resources..."); 
} 
} 
 
 
1 7 . 4 Implementing HttpSessionListener 
The interface is used to receive notifications about changes 
to the HTTP session lifecycle. This is useful for tracking session creation and 
destruction events. 

=== PAGE 91 ===
Servlet 88 
 
 
 
Example: HttpSessionListener 
 
import javax.servlet.annotation.WebListener; 
import javax.servlet.http.HttpSessionEvent; 
import javax.servlet.http.HttpSessionListener; 
 
@WebListener 
public class MyAppHttpSessionListener implements HttpSessio 
nListener { 
 
@Override 
public void sessionCreated(HttpSessionEvent se) { 
// Code to handle session creation 
System.out.println("Session created: " + se.getSess 
ion().getId()); 
} 
 
@Override 
public void sessionDestroyed(HttpSessionEvent se) { 
// Code to handle session destruction 
System.out.println("Session destroyed: " + se.getSe 
ssion().getId()); 
} 
} 
 
 
1 7 . 5 Implementing ServletRequestListener 
The interface is used to receive notifications about 
changes to the request lifecycle. This is useful for tracking request initialization 
and destruction events. 
Example: ServletRequestListener 
 
import javax.servlet.ServletRequestEvent; 
import javax.servlet.ServletRequestListener; 
import javax.servlet.annotation.WebListener; 
 
@WebListener 
public class MyAppServletRequestListener implements Servlet 

=== PAGE 92 ===
Servlet 89 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
1 7 . 6 Other Specialized Listeners 
In addition to the basic listeners, there are specialized listeners for more 
granular control over events within the servlet context, session, and request 
scopes: 
1. ServletContextAttributeListener: Listens for changes to attributes in the 
servlet context. 
2. HttpSessionAttributeListener: Listens for changes to attributes in HTTP 
sessions. 
3. ServletRequestAttributeListener: Listens for changes to attributes in 
servlet requests. 
Example: HttpSessionAttributeListener 
 
import javax.servlet.annotation.WebListener; 
import javax.servlet.http.HttpSessionAttributeListener; 
import javax.servlet.http.HttpSessionBindingEvent; 
 
@WebListener 
public class MyAppHttpSessionAttributeListener implements H 
RequestListener { 
@Override 
public void requestInitialized(ServletRequestEvent sre) 
{ 
// Code to handle request initialization 
System.out.println("Request initialized: " + sre.ge 
tServletRequest().getRemoteAddr()); 
} 
@Override 
public void requestDestroyed(ServletRequestEvent sre) { 
// Code to handle request destruction 
System.out.println("Request destroyed: " + sre.getS 
ervletRequest().getRemoteAddr()); 
} 
} 

=== PAGE 93 ===
Servlet 90 
 
 
ttpSessionAttributeListener { 
 
@Override 
public void attributeAdded(HttpSessionBindingEvent even 
t) { 
// Code to handle attribute addition 
System.out.println("Session attribute added: " + ev 
ent.getName() + "=" + event.getValue()); 
} 
 
@Override 
public void attributeRemoved(HttpSessionBindingEvent ev 
ent) { 
// Code to handle attribute removal 
System.out.println("Session attribute removed: " + 
event.getName()); 
} 
 
@Override 
public void attributeReplaced(HttpSessionBindingEvent e 
vent) { 
// Code to handle attribute replacement 
System.out.println("Session attribute replaced: " + 
event.getName() + "=" + event.getValue()); 
} 
} 
 
 
1 7 . 7 Practical Use Cases 
Example: Application Initialization 
import javax.servlet.ServletContextEvent; 
import javax.servlet.ServletContextListener; 
import javax.servlet.annotation.WebListener; 
@WebListener 
public class DatabaseInitListener implements ServletContext 
Listener { 

=== PAGE 94 ===
Servlet 91 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
Example: Session Tracking 
 
import javax.servlet.annotation.WebListener; 
import javax.servlet.http.HttpSessionEvent; 
import javax.servlet.http.HttpSessionListener; 
 
@WebListener 
public class ActiveUserCounter implements HttpSessionListen 
er { 
 
private static int activeSessions = 0; 
 
@Override 
public void sessionCreated(HttpSessionEvent se) { 
activeSessions++; 
System.out.println("Session created. Active session 
s: " + activeSessions); 
} 
 
@Override 
public void sessionDestroyed(HttpSessionEvent se) { 
activeSessions--; 
@Override 
public void contextInitialized(ServletContextEvent sce) 
{ 
// Initialize database connection pool 
System.out.println("Initializing database connectio 
n pool..."); 
} 
@Override 
public void contextDestroyed(ServletContextEvent sce) { 
// Close database connection pool 
System.out.println("Closing database connection poo 
l..."); 
} 
} 

=== PAGE 95 ===
Servlet 92 
 
 
 
 
 
 
 
 
 
 
 
 
1 7 . 8 Summary 
Servlet listeners provide a powerful mechanism to intercept and handle events 
in the lifecycle of a web application, HTTP sessions, and requests. By 
understanding and implementing listeners, developers can perform important 
tasks such as initializing resources, tracking user sessions, and monitoring 
request activities, thus making the web application more robust and 
maintainable. 
 
Chapter 18: Advanced Topics and 
Techniques 
1 8 . 1 Asynchronous Servlets 
Introduction 
Asynchronous processing in servlets allows you to handle long -running tasks 
without blocking the server's resources. This technique is particularly useful for 
web applications requiring tasks such as file uploads, web service calls, and  
other operations that might take a considerable amount of time to complete. 
 
Explanation 
In traditional servlet processing, each request is handled by a single thread. 
This approach can lead to resource exhaustion when many long-running tasks 
are processed simultaneously. Asynchronous servlets help by releasing the 
initial request thread back to the container, which can then be reused for other 
tasks. 
Example 
System.out.println("Session destroyed. Active sessi 
ons: " + activeSessions); 
} 
public static int getActiveSessions() { 
return activeSessions; 
} 
} 

=== PAGE 96 ===
Servlet 93 
 
 
 
 
Consider an example where a servlet performs a time-consuming database 
operation. Using asynchronous processing, the servlet can delegate this 
operation to a separate thread, freeing the request thread for other tasks. 
Code 
 
@WebServlet(urlPatterns = "/async", asyncSupported = true) 
public class AsyncServlet extends HttpServlet { 
 
@Override 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
 
 
 
 
 
 
 
 
 
d!"); 
AsyncContext asyncContext = request.startAsync(); 
asyncContext.start(() -> { 
try { 
// Simulate long-running task 
Thread.sleep(5000); 
response.getWriter().write("Task Complete 
 
} catch (Exception e) { 
e.printStackTrace(); 
} finally { 
asyncContext.complete(); 
} 
}); 
} 
} 
 
In this example, the annotation indicates that asynchronous 
processing is supported. The method initiates asynchronous 
processing, and the task is performed in a new thread. 
1 8 . 2 WebSockets and Servlets 
Introduction 
WebSockets provide a full-duplex communication channel over a single TCP 
connection. This technology is suitable for real-time web applications, such as 

=== PAGE 97 ===
Servlet 94 
 
 
chat applications, live updates, and gaming. 
 
Explanation 
Unlike HTTP, which is a request-response protocol, WebSockets enable 
continuous data exchange between the client and server. This allows for more 
interactive and dynamic web applications. 
Example 
Consider an example of a simple chat application using WebSockets in servlets. 
 
Code 
@ServerEndpoint("/chat") 
public class ChatEndpoint { 
@OnOpen 
public void onOpen(Session session) { 
System.out.println("Connected: " + session.getId 
()); 
} 
@OnMessage 
public void onMessage(String message, Session session) 
{ 
try { 
for (Session s : session.getOpenSessions()) { 
s.getBasicRemote().sendText(message); 
} 
} catch (IOException e) { 
e.printStackTrace(); 
} 
} 
@OnClose 
public void onClose(Session session) { 
System.out.println("Disconnected: " + session.getId 
()); 

=== PAGE 98 ===
Servlet 95 
 
 
 
 
 
 
 
In this example, the annotation is used to define a WebSocket 
endpoint. The 
lifecycle events. 
, , and methods handle the WebSocket 
1 8 . 3 Handling File Uploads 
Introduction 
File upload functionality is a common requirement in web applications. Servlets 
provide mechanisms to handle multipart/form-data requests, which are used 
for file uploads. 
Explanation 
The interface is used to handle file upload in servlets. It 
provides methods to access file content, file name, and other metadata. 
 
Example 
Consider an example where a servlet handles file upload and saves the 
uploaded file to a server directory. 
Code 
} 
} 
@WebServlet("/upload") 
@MultipartConfig 
public class FileUploadServlet extends HttpServlet { 
@Override 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) 
throws ServletException, IOException { 
Part filePart = request.getPart("file"); 
String fileName = Paths.get(filePart.getSubmittedFi 
leName()).getFileName().toString(); 
InputStream fileContent = filePart.getInputStream 
(); 
 
  
  

=== PAGE 99 ===
Servlet 96 
 
 
@Stateless 
public class DataBean { 
 
 
 
 
 
 
 
 
 
 
 
 
 
In this example, the annotation is used to specify that the 
servlet handles multipart/form-data requests. The 
method retrieves the uploaded file, and the file content is saved to the server. 
1 8 . 4 Integrating Servlets with Other Java EE Technologies 
Introduction 
Servlets can be integrated with other Java EE technologies such as JavaServer 
Pages (JSP), Enterprise JavaBeans (EJB), and Java Persistence API (JPA) to  
build robust web applications. 
 
Explanation 
Integrating servlets with JSP allows for a separation of concerns, where 
servlets handle the business logic and JSPs handle the presentation logic. EJB 
can be used for handling business logic and transactions, while JPA provides a 
mechanism for interacting with databases. 
Example 
Consider an example where a servlet interacts with an EJB to retrieve data from 
a database using JPA and forwards the data to a JSP for rendering. 
Code 
EJB 
File file = new File("uploads/" + fileName); 
Files.copy(fileContent, file.toPath(), StandardCopy 
Option.REPLACE_EXISTING); 
response.getWriter().write("File uploaded successfu 
lly!"); 
} 
} 
 

=== PAGE 100 ===
Servlet 97 
 
 
 
 
 
 
 
 
 
 
 
Servlet 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
JSP (data.jsp) 
@PersistenceContext 
private EntityManager entityManager; 
public List<Data> getData() { 
return entityManager.createQuery("SELECT d FROM Dat 
a d", Data.class).getResultList(); 
} 
} 
@WebServlet("/data") 
public class DataServlet extends HttpServlet { 
@EJB 
private DataBean dataBean; 
@Override 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
List<Data> dataList = dataBean.getData(); 
request.setAttribute("dataList", dataList); 
request.getRequestDispatcher("/data.jsp").forward(r 
equest, response); 
} 
} 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 
<head> 
<title>Data List</title> 
</head> 

=== PAGE 101 ===
Servlet 98 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
In this example, the EJB retrieves data from the database. The 
calls the EJB and forwards the data to a JSP for rendering. 
 
Conclusion 
In this chapter, we've explored some advanced topics and techniques in servlet 
programming, including asynchronous servlets, WebSockets, file uploads, and 
integration with other Java EE technologies. These techniques enhance the 
capabilities of web applications, making them more responsive, interactive, and 
scalable. 
 
Chapter 19: Best Practices and Security 
1 9 . 1 Best Practices for Servlet Programming 
Introduction 
Adhering to best practices in servlet programming ensures your applications 
are efficient, maintainable, and scalable. This section covers key practices to 
follow when developing servlets. 
Explanation and Examples 
1 9 . 1 . 1 Follow MVC Architecture 
The Model-View-Controller (MVC) architecture separates the application logic 
into three components: 
 Model: Represents the data and business logic. 
 View: Represents the presentation layer. 
<body> 
<h1>Data List</h1> 
<ul> 
<c:forEach var="data" items="${dataList}"> 
<li>${data.name}</li> 
</c:forEach> 
</ul> 
</body> 
</html> 
 

=== PAGE 102 ===
Servlet 99 
 
 
 Controller: Handles user input and interacts with the model to render the 
view. 
Example 
 Model: EJB or POJO classes. 
 View: JSP pages. 
 Controller: Servlets. 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
1 9 . 1 . 2 Use Dependency Injection 
Use dependency injection to manage the lifecycle of components and their 
dependencies, making your code cleaner and easier to test. 
Example 
 
 
 
 
1 9 . 1 . 3 Handle Exceptions Gracefully 
@WebServlet("/user") 
public class UserController extends HttpServlet { 
@EJB 
private UserService userService; 
@Override 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
List<User> users = userService.getAllUsers(); 
request.setAttribute("users", users); 
request.getRequestDispatcher("/userList.jsp").forwa 
rd(request, response); 
} 
} 
@EJB 
private UserService userService; 

=== PAGE 103 ===
Servlet 100 
 
 
Always handle exceptions properly to avoid exposing sensitive information and 
to ensure a smooth user experience. 
Example 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
1 9 . 1 . 4 Optimize Performance 
Optimize performance by minimizing the use of synchronization, avoiding 
expensive operations in servlets, and using caching where appropriate. 
Example 
 Use local variables instead of instance variables for thread safety. 
 Cache frequently accessed data. 
1 9 . 2 Servlet Security Considerations 
Introduction 
Security is paramount in web applications. This section discusses various 
security considerations and best practices to secure your servlet -based 
applications. 
Explanation and Examples 
1 9 . 2 . 1 Secure Communication (HTTPS) 
@Override 
protected void doGet(HttpServletRequest request, HttpServle 
tResponse response) 
throws ServletException, IOException { 
try { 
// Business logic 
} catch (Exception e) { 
log("Error processing request", e); 
response.sendError(HttpServletResponse.SC_INTERNAL_ 
SERVER_ERROR, "An unexpected error occurred"); 
} 
} 

=== PAGE 104 ===
Servlet 101 
 
 
Always use HTTPS to encrypt data transmitted between the client and server, 
protecting sensitive information from eavesdropping and tampering. 
Example 
Configure your web server (e.g., Apache, Nginx) to use HTTPS with a valid 
SSL/TLS certificate. 
1 9 . 2 . 2 Validate and Sanitize Input 
Always validate and sanitize user inputs to prevent common security 
vulnerabilities such as SQL injection, XSS, and CSRF.  
Example 
 SQL Injection Prevention: Use prepared statements. 
 
 
 
 
 
 
 
 
 XSS Prevention: Encode user inputs before displaying them in the browser. 
 
 
 
 
1 9 . 2 . 3 Implement Access Control 
Ensure that sensitive parts of your application are accessible only to authorized 
users. 
Example 
 Declarative Security: Use deployment descriptors (web.xml) to define 
security constraints. 
String query = "SELECT * FROM users WHERE username = ?"; 
PreparedStatement stmt = connection.prepareStatement(que 
ry); 
stmt.setString(1, username); 
ResultSet rs = stmt.executeQuery(); 
<c:out value="${userInput}" /> 
<security-constraint> 
<web-resource-collection> 
<web-resource-name>Secure Area</web-resource-nam 
e> 
<url-pattern>/secure/*</url-pattern> 

=== PAGE 105 ===
Servlet 102 
 
 
 
 
 
 
 
 
 
 Programmatic Security: Check user roles programmatically in servlets. 
 
 
 
 
 
 
 
 
1 9 . 2 . 4 Use Secure Cookies 
Set secure attributes on cookies to prevent them from being accessed by 
malicious scripts. 
Example 
 
 
 
 
 
1 9 . 3 Preventing Common Security Vulnerabilities 
Introduction 
This section covers techniques to prevent common security vulnerabilities in 
servlet-based applications. 
Explanation and Examples 
1 9 . 3 . 1 Cross-Site Scripting (XSS) 
XSS attacks occur when an attacker injects malicious scripts into a web 
application, which are then executed in the context of the user's browser. 
</web-resource-collection> 
<auth-constraint> 
<role-name>ADMIN</role-name> 
</auth-constraint> 
</security-constraint> 
if (request.isUserInRole("ADMIN")) { 
// Allow access 
} else { 
response.sendError(HttpServletResponse.SC_FORBIDDEN, 
"Access Denied"); 
} 
Cookie sessionCookie = new Cookie("JSESSIONID", sessionId); 
sessionCookie.setHttpOnly(true); 
sessionCookie.setSecure(true); 
response.addCookie(sessionCookie); 

=== PAGE 106 ===
Servlet 103 
 
 
Prevention 
 Encode output data. 
 Use libraries like OWASP's Java Encoder. 
 
1 9 . 3 . 2 Cross-Site Request Forgery (CSRF) 
CSRF attacks occur when a malicious website tricks a user's browser into 
performing actions on another website where the user is authenticated. 
Prevention 
 Use CSRF tokens in forms and validate them on the server-side. 
 
Example 
 
 
 
 
 
 
 
 
 
1 9 . 3 . 3 SQL Injection 
SQL injection attacks occur when an attacker inserts malicious SQL queries into 
a web application's input fields. 
Prevention 
 Use prepared statements and parameterized queries. 
 Validate and sanitize inputs. 
 
Conclusion 
In this chapter, we've covered best practices and security considerations for 
servlet programming. By following these guidelines, you can develop secure, 
efficient, and maintainable servlet-based applications. Always stay updated 
with the latest security trends and continuously review and improve your 
codebase to address potential vulnerabilities. 
String csrfToken = (String) session.getAttribute("csrfToke 
n"); 
if (csrfToken == null || !csrfToken.equals(request.getParam 
eter("csrfToken"))) { 
response.sendError(HttpServletResponse.SC_FORBIDDEN, "I 
nvalid CSRF token"); 
} 

=== PAGE 107 ===
Servlet 104 
 
 
Chapter 20: Case Studies and Projects 
2 0 . 1 Introduction 
This chapter provides practical examples and case studies that illustrate how to 
build robust servlet-based web applications. These case studies cover a  
variety of scenarios, demonstrating the application of servlets in real-world 
projects. Each case study includes detailed explanations, code samples, and 
best practices. 
2 0 . 2 Case Study 1: Building an Online Bookstore 
Introduction 
This case study demonstrates how to build an online bookstore using servlets, 
JSP, and a database. The bookstore allows users to browse books, add them to 
a cart, and proceed to checkout. 
Requirements 
 User registration and login. 
 Browsing and searching for books. 
 Adding books to a shopping cart. 
 Proceeding to checkout and placing an order. 
 Admin interface for managing books and orders. 
 
Solution 
2 0 . 2 . 1 Database Schema 
Create a database schema for storing users, books, and orders. 
 
SQL Script 
CREATE TABLE users ( 
id INT PRIMARY KEY AUTO_INCREMENT, 
username VARCHAR(50) UNIQUE NOT NULL, 
password VARCHAR(50) NOT NULL, 
email VARCHAR(100) NOT NULL 
); 

=== PAGE 108 ===
Servlet 105 
 
 
 
CREATE TABLE books ( 
id INT PRIMARY KEY AUTO_INCREMENT, 
title VARCHAR(100) NOT NULL, 
author VARCHAR(100) NOT NULL, 
price DECIMAL(10, 2) NOT NULL, 
description TEXT, 
image_url VARCHAR(255) 
); 
 
CREATE TABLE orders ( 
id INT PRIMARY KEY AUTO_INCREMENT, 
user_id INT NOT NULL, 
total DECIMAL(10, 2) NOT NULL, 
status VARCHAR(20) NOT NULL, 
created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, 
FOREIGN KEY (user_id) REFERENCES users(id) 
); 
 
CREATE TABLE order_items ( 
id INT PRIMARY KEY AUTO_INCREMENT, 
order_id INT NOT NULL, 
book_id INT NOT NULL, 
quantity INT NOT NULL, 
FOREIGN KEY (order_id) REFERENCES orders(id), 
FOREIGN KEY (book_id) REFERENCES books(id) 
); 
 
 
2 0 . 2 . 2 User Registration and Login 
Create servlets for user registration and login. 
 
RegistrationServlet.java 
@WebServlet("/register") 
public class RegistrationServlet extends HttpServlet { 
@Override 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) 

=== PAGE 109 ===
Servlet 106 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
LoginServlet.java 
 
@WebServlet("/login") 
public class LoginServlet extends HttpServlet { 
@Override 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) 
throws ServletException, IOException { 
String username = request.getParameter("username"); 
String password = request.getParameter("password"); 
 
 
 
 
word); 
UserService userService = new UserService(); 
User user = userService.authenticate(username, pass 
 
if (user != null) { 
HttpSession session = request.getSession(); 
session.setAttribute("user", user); 
response.sendRedirect("home.jsp"); 
} else { 
response.sendRedirect("login.jsp?error=Invalid 
credentials"); 
} 
throws ServletException, IOException { 
String username = request.getParameter("username"); 
String password = request.getParameter("password"); 
String email = request.getParameter("email"); 
// Save user to database 
User user = new User(username, password, email); 
UserService userService = new UserService(); 
userService.registerUser(user); 
response.sendRedirect("login.jsp"); 
} 
} 

=== PAGE 110 ===
Servlet 107 
 
 
 
 
 
2 0 . 2 . 3 Browsing and Searching for Books 
Create servlets and JSP pages to display and search for books. 
 
BookServlet.java 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
books.jsp 
} 
} 
@WebServlet("/books") 
public class BookServlet extends HttpServlet { 
@Override 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
BookService bookService = new BookService(); 
List<Book> books = bookService.getAllBooks(); 
request.setAttribute("books", books); 
request.getRequestDispatcher("books.jsp").forward(r 
equest, response); 
} 
} 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 
<head> 
<title>Books</title> 
</head> 
<body> 
<h1>Book List</h1> 
<c:forEach var="book" items="${books}"> 
<div> 
<img src="${book.imageUrl}" alt="${book.title}" 
/> 

=== PAGE 111 ===
Servlet 108 
 
 
 
 
 
 
 
 
 
 
 
 
2 0 . 2 . 4 Shopping Cart 
Create servlets for managing the shopping cart. 
 
CartServlet.java 
<h2>${book.title}</h2> 
<p>by ${book.author}</p> 
<p>${book.price}</p> 
<a href="cart?bookId=${book.id}">Add to Cart</a 
> 
</div> 
</c:forEach> 
</body> 
</html> 
@WebServlet("/cart") 
public class CartServlet extends HttpServlet { 
@Override 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
HttpSession session = request.getSession(); 
Cart cart = (Cart) session.getAttribute("cart"); 
if (cart == null) { 
cart = new Cart(); 
session.setAttribute("cart", cart); 
} 
 
String bookId = request.getParameter("bookId"); 
if (bookId != null) { 
BookService bookService = new BookService(); 
Book book = bookService.getBookById(Integer.par 
seInt(bookId)); 
cart.addBook(book); 
} 
 
request.setAttribute("cart", cart); 
request.getRequestDispatcher("cart.jsp").forward(re 

=== PAGE 112 ===
Servlet 109 
 
 
 
cart.jsp 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
2 0 . 2 . 5 Checkout and Order Processing 
Create servlets for handling checkout and processing orders. 
 
CheckoutServlet.java 
 
quest, response); 
} 
} 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 
<head> 
<title>Shopping Cart</title> 
</head> 
<body> 
<h1>Your Cart</h1> 
<c:forEach var="item" items="${cart.items}"> 
<div> 
<img src="${item.book.imageUrl}" alt="${item.bo 
ok.title}" /> 
<h2>${item.book.title}</h2> 
<p>${item.book.price}</p> 
<p>Quantity: ${item.quantity}</p> 
</div> 
</c:forEach> 
<a href="checkout">Checkout</a> 
</body> 
</html> 
@WebServlet("/checkout") 
public class CheckoutServlet extends HttpServlet { 
@Override 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 

=== PAGE 113 ===
Servlet 110 
 
 
 
orderConfirmation.jsp 
 
 
 
 
 
 
 
 
 
 
 
 
 
2 0 . 2 . 6 Admin Interface 
Create servlets and JSP pages for the admin interface to manage books and 
orders. 
AdminBookServlet.java 
throws ServletException, IOException { 
HttpSession session = request.getSession(); 
Cart cart = (Cart) session.getAttribute("cart"); 
User user = (User) session.getAttribute("user"); 
 
if (cart != null && user != null) { 
OrderService orderService = new OrderService(); 
orderService.placeOrder(user, cart); 
 
session.removeAttribute("cart"); 
response.sendRedirect("orderConfirmation.jsp"); 
} else { 
response.sendRedirect("cart"); 
} 
} 
} 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 
<head> 
<title>Order Confirmation</title> 
</head> 
<body> 
<h1>Thank you for your order!</h1> 
<p>Your order has been placed successfully.</p> 
</body> 
</html> 

=== PAGE 114 ===
Servlet 111 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
adminBooks.jsp 
 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 
<head> 
<title>Admin - Manage Books</title> 
</head> 
<body> 
<h1>Manage Books</h1> 
<a href="addBook.jsp">Add New Book</a> 
<c:forEach var="book" items="${books}"> 
<div> 
<img src="${book.imageUrl}" alt="${book.title}" 
/> 
<h2>${book.title}</h2> 
<p>by ${book.author}</p> 
<p>${book.price}</p> 
<a href="editBook?id=${book.id}">Edit</a> 
<a href="deleteBook?id=${book.id}">Delete</a> 
</div> 
</c:forEach> 
@WebServlet("/admin/books") 
public class AdminBookServlet extends HttpServlet { 
@Override 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
BookService bookService = new BookService(); 
List<Book> books = bookService.getAllBooks(); 
 
request.setAttribute("books", books); 
request.getRequestDispatcher("adminBooks.jsp").forw 
ard(request, response); 
} 
} 

=== PAGE 115 ===
Servlet 112 
 
 
 
 
2 0 . 3 Case Study 2: Building a Job Portal 
Introduction 
This case study demonstrates how to build a job portal using servlets, JSP, and 
a database. The job portal allows employers to post job openings and job 
seekers to apply for jobs. 
Requirements 
 Employer registration and login. 
 Job posting and management. 
 Job seeker registration and login. 
 Job search and application. 
 Admin interface for managing users and job postings. 
 
Solution 
2 0 . 3 . 1 Database Schema 
Create a database schema for storing users, jobs, and applications. 
 
SQL Script 
</body> 
</html> 
CREATE TABLE users ( 
id INT PRIMARY KEY AUTO_INCREMENT 
 
, 
username VARCHAR(50) UNIQUE NOT NULL, 
password VARCHAR(50) NOT NULL, 
email VARCHAR(100) NOT NULL, 
role VARCHAR(20) NOT NULL 
); 
 
CREATE TABLE jobs ( 
id INT PRIMARY KEY AUTO_INCREMENT, 

=== PAGE 116 ===
Servlet 113 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
2 0 . 3 . 2 Employer Registration and Login 
Create servlets for employer registration and login. 
 
EmployerRegistrationServlet.java 
employer_id INT NOT NULL, 
title VARCHAR(100) NOT NULL, 
description TEXT NOT NULL, 
location VARCHAR(100) NOT NULL, 
posted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, 
FOREIGN KEY (employer_id) REFERENCES users(id) 
); 
CREATE TABLE applications ( 
id INT PRIMARY KEY AUTO_INCREMENT, 
job_id INT NOT NULL, 
seeker_id INT NOT NULL, 
cover_letter TEXT, 
applied_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, 
FOREIGN KEY (job_id) REFERENCES jobs(id), 
FOREIGN KEY (seeker_id) REFERENCES users(id) 
); 
@WebServlet("/employer/register") 
public class EmployerRegistrationServlet extends HttpServle 
t { 
@Override 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) 
throws ServletException, IOException { 
String username = request.getParameter("username"); 
String password = request.getParameter("password"); 
String email = request.getParameter("email"); 
// Save employer to database 
User employer = new User(username, password, email, 
"EMPLOYER"); 
UserService userService = new UserService(); 

=== PAGE 117 ===
Servlet 114 
 
 
 
 
 
 
 
 
EmployerLoginServlet.java 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
2 0 . 3 . 3 Job Posting and Management 
Create servlets for posting and managing job listings. 
 
JobPostingServlet.java 
userService.registerUser(employer); 
response.sendRedirect("login.jsp"); 
} 
} 
@WebServlet("/employer/login") 
public class EmployerLoginServlet extends HttpServlet { 
@Override 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) 
throws ServletException, IOException { 
String username = request.getParameter("username"); 
String password = request.getParameter("password"); 
 
UserService userService = new UserService(); 
User user = userService.authenticate(username, pass 
word); 
 
if (user != null && "EMPLOYER".equals(user.getRole 
())) { 
HttpSession session = request.getSession(); 
session.setAttribute("user", user); 
response.sendRedirect("dashboard.jsp"); 
} else { 
response.sendRedirect("login.jsp?error=Invalid 
credentials"); 
} 
} 
} 

=== PAGE 118 ===
Servlet 115 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
jobPosting.jsp 
 
@WebServlet("/employer/postJob") 
public class JobPostingServlet extends HttpServlet { 
@Override 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) 
throws ServletException, IOException { 
HttpSession session = request.getSession(); 
User employer = (User) session.getAttribute("use 
r"); 
 
String title = request.getParameter("title"); 
String description = request.getParameter("descript 
ion"); 
String location = request.getParameter("location"); 
 
Job job = new Job(employer.getId(), title, descript 
ion, location); 
JobService jobService = new JobService(); 
jobService.postJob(job); 
 
response.sendRedirect("dashboard.jsp"); 
} 
} 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 
<head> 
<title>Post Job</title> 
</head> 
<body> 
<h1>Post a New Job</h1> 
<form action="postJob" method="post"> 
<label for="title">Job Title:</label> 
<input type="text" name="title" id="title" required 

=== PAGE 119 ===
Servlet 116 
 
 
 
2 0 . 3 . 4 Job Search and Application 
Create servlets for job search and application. 
 
JobSearchServlet.java 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
jobs.jsp 
 
><br> 
<label for="description">Job Description:</label> 
<textarea name="description" id="description" requi 
red></textarea><br> 
<label for="location">Location:</label> 
<input type="text" name="location" id="location" re 
quired><br> 
<input type="submit" value="Post Job"> 
</form> 
</body> 
</html> 
@WebServlet("/jobs") 
public class JobSearchServlet extends HttpServlet { 
@Override 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 
throws ServletException, IOException { 
JobService jobService = new JobService(); 
List<Job> jobs = jobService.getAllJobs(); 
 
request.setAttribute("jobs", jobs); 
request.getRequestDispatcher("jobs.jsp").forward(re 
quest, response); 
} 
} 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 

=== PAGE 120 ===
Servlet 117 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
JobApplicationServlet.java 
 
@WebServlet("/apply") 
public class JobApplicationServlet extends HttpServlet { 
@Override 
protected void doPost(HttpServletRequest request, HttpS 
ervletResponse response) 
throws ServletException, IOException { 
HttpSession session = request.getSession(); 
User seeker = (User) session.getAttribute("user"); 
 
int jobId = Integer.parseInt(request.getParameter 
("jobId")); 
String coverLetter = request.getParameter("coverLet 
ter"); 
 
Application application = new Application(jobId, se 
eker.getId(), coverLetter); 
ApplicationService applicationService = new Applica 
tionService(); 
applicationService.applyForJob(application); 
<head> 
<title>Job Listings</title> 
</head> 
<body> 
<h1>Available Jobs</h1> 
<c:forEach var="job" items="${jobs}"> 
<div> 
<h2>${job.title}</h2> 
<p>${job.description}</p> 
<p>Location: ${job.location}</p> 
<a href="apply?jobId=${job.id}">Apply</a> 
</div> 
</c:forEach> 
</body> 
</html> 

=== PAGE 121 ===
Servlet 118 
 
 
 
 
 
 
 
apply.jsp 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
2 0 . 3 . 5 Admin Interface 
Create servlets and JSP pages for the admin interface to manage users and job 
postings. 
AdminUserServlet.java 
 
response.sendRedirect("jobs.jsp?message=Application 
submitted"); 
} 
} 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 
<head> 
<title>Apply for Job</title> 
</head> 
<body> 
<h1>Apply for Job</h1> 
<form action="apply" method="post"> 
<input type="hidden" name="jobId" value="${job.i 
d}"> 
<label for="coverLetter">Cover Letter:</label> 
<textarea name="coverLetter" id="coverLetter" requi 
red></textarea><br> 
<input type="submit" value="Apply"> 
</form> 
</body> 
</html> 
@WebServlet("/admin/users") 
public class AdminUserServlet extends HttpServlet { 
@Override 
protected void doGet(HttpServletRequest request, HttpSe 
rvletResponse response) 

=== PAGE 122 ===
Servlet 119 
 
 
 
adminUsers.jsp 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
2 0 . 4 Conclusion 
This chapter covered two detailed case studies demonstrating how to build 
complex web applications using servlets. These examples should provide a 
solid foundation for building your own servlet-based projects. Experiment with 
throws ServletException, IOException { 
UserService userService = new UserService(); 
List<User> users = userService.getAllUsers(); 
 
request.setAttribute("users", users); 
request.getRequestDispatcher("adminUsers.jsp").forw 
ard(request, response); 
} 
} 
<%@ page contentType="text/html;charset=UTF-8" language="ja 
va" %> 
<html> 
<head> 
<title>Admin - Manage Users</title> 
</head> 
<body> 
<h1>Manage Users</h1> 
<c:forEach var="user" items="${users}"> 
<div> 
<h2>${user.username}</h2> 
<p>${user.email}</p> 
<p>Role: ${user.role}</p> 
<a href="editUser?id=${user.id}">Edit</a> 
<a href="deleteUser?id=${user.id}">Delete</a> 
</div> 
</c:forEach> 
</body> 
</html> 

=== PAGE 123 ===
Servlet 120 
 
 
these case studies, customize them to fit your needs, and explore additional 
features to enhance your applications further. 
 
Chapter 21: References and Further 
Learning 
2 1 . 1 Introduction 
This chapter provides a curated list of books, websites, and other resources for 
further learning about Servlets and related technologies. Whether you are a 
beginner or an experienced developer, these references will help you deepen 
your understanding and stay updated with the latest advancements in web 
development with Java. 
 
2 1 . 2 Books 
1. "Head First Servlets and JSP" by Bryan Basham, Kathy Sierra, and Bert 
Bates 
 A comprehensive and engaging guide to learning Servlets and JSP, 
covering the fundamentals and advanced topics with practical 
examples and exercises. 
2. "Java Servlet Programming" by Jason Hunter and William Crawford 
 This book provides an in-depth look at servlet programming, covering 
core concepts, advanced techniques, and real -world examples. 
3. "Core Servlets and JavaServer Pages: Volume 1: Core Technologies" by 
Marty Hall and Larry Brown 
 A detailed guide to core servlet and JSP technologies, offering 
extensive coverage of essential topics and practical examples. 
4. "Java EE 8 Application Development" by David R. Heffelfinger 
 A comprehensive guide to Java EE 8, covering various technologies 
including Servlets, JSP, and other related frameworks and tools. 
5. "Servlet & JSP: A Tutorial" by Budi Kurniawan 
 This tutorial-style book provides a step-by-step approach to learning 
Servlets and JSP, with plenty of code examples and explanations. 

=== PAGE 124 ===
Servlet 121 
 
 
2 1 . 3 Websites and Online Tutorials 
1. Oracle Java EE Documentation 
 The official documentation for Java EE, including detailed guides and 
references for Servlets and other related technologies.  
 Oracle Java EE Documentation 
2. Baeldung 
 A comprehensive resource for Java developers, offering tutorials and 
articles on a wide range of Java technologies, including Servlets.  
 Baeldung 
3. Servlets and JSP on TutorialsPoint 
 A well-structured tutorial covering the basics and advanced concepts 
of Servlets and JSP with practical examples.  
 TutorialsPoint - Servlets and JSP 
4. JavaBrains 
 A popular online learning platform offering video tutorials on various 
Java technologies, including Servlets and JSP.  
 JavaBrains 
5. Vogella 
 A comprehensive site offering tutorials and guides on Java 
development, including detailed articles on Servlets and JSP. 
 Vogella - Servlets 
 
2 1 . 4 Online Courses and MOOCs 
1. Coursera - Java Programming and Software Engineering Fundamentals 
Specialization 
 A series of courses offered by Duke University, covering Java 
programming, software engineering fundamentals, and web 
development. 
 Coursera - Java Programming and Software Engineering Fundamentals 
2. Udemy - Java Servlets and JSP - Build Java EE(JEE) Web Applications 

=== PAGE 125 ===
Servlet 122 
 
 
 A practical course on Udemy that teaches how to build Java EE web 
applications using Servlets and JSP. 
 Udemy - Java Servlets and JSP 
3. Pluralsight - Java EE: Building a Web App with Java Servlets 
 A course on Pluralsight focusing on building web applications using 
Java Servlets, covering both basics and advanced topics.  
 Pluralsight - Java EE: Building a Web App with Java Servlets 
4. edX - Introduction to Java EE 
 An introductory course on edX that covers the fundamentals of Java EE, 
including Servlets and other key technologies.  
 edX - Introduction to Java EE 
 
2 1 . 5 Community and Forums 
1. Stack Overflow 
 A popular question-and-answer site where developers can ask 
questions, share knowledge, and learn from the community. 
 Stack Overflow - Servlets 
2. JavaRanch (Coderanch) 
 An online community for Java developers, offering forums, articles, and 
resources on various Java topics, including Servlets.  
 JavaRanch 
3. Reddit - r/java 
 A subreddit for Java developers to discuss news, share resources, and 
ask for help with Java-related questions. 
 Reddit - r/java 
4. Oracle Java Community 
 The official Java community site by Oracle, providing forums, blogs, 
and resources for Java developers. 
 Oracle Java Community 
 
2 1 . 6 Further Learning and Advanced Topics 

=== PAGE 126 ===
Servlet 123 
 
 
1. Understanding Java EE Architecture 
 Explore the architectural concepts and patterns used in Java EE 
applications, including MVC, dependency injection, and microservices. 
2. Exploring Other Java EE Technologies 
 Learn about other Java EE technologies such as JSF (JavaServer 
Faces), JPA (Java Persistence API), and EJB (Enterprise JavaBeans) to 
build more comprehensive applications. 
3. Integrating Modern Front-end Frameworks 
 Integrate modern front-end frameworks like Angular, React, or Vue.js 
with Java EE backends to create dynamic and responsive web 
applications. 
4. Asynchronous Processing and WebSockets 
 Dive deeper into asynchronous processing and real -time 
communication using Servlets, WebSockets, and other related 
technologies. 
5. Cloud Deployment and DevOps 
 Explore cloud deployment options for Java EE applications and learn 
about DevOps practices for continuous integration and continuous 
deployment (CI/CD). 
2 1 . 7 Conclusion 
The resources listed in this chapter provide a comprehensive guide for further 
learning and mastering Servlets and Java EE technologies. Whether you prefer 
books, online tutorials, courses, or community interaction, these references will 
support your ongoing journey in web development with Java. Stay curious, 
keep experimenting, and continue to build on the foundational knowledge you 
have gained throughout this book. 

